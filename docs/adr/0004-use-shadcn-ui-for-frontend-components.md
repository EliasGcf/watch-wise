# Use shadcn/ui for frontend components

Watch Wise will use shadcn/ui source components for shared frontend building blocks such as buttons, cards, fields, inputs, alerts, and badges. This gives the React/Inertia UI a consistent component language while keeping component code local and adaptable to the app's visual direction.
