// This file is auto-generated by @hey-api/openapi-ts
// @ts-nocheck

import { faker, type Faker } from '@faker-js/faker';

import type { Delete3AuthenticationSessionData, Delete3AuthenticationSessionResponse, Delete3ListByListIdData, Delete3ListByListIdResponse, Delete3MovieByMovieIdRatingData, Delete3MovieByMovieIdRatingResponse, Delete3TvBySeriesIdRatingData, Delete3TvBySeriesIdRatingResponse, Delete3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberRatingData, Delete3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberRatingResponse, Get3AccountByAccountIdData, Get3AccountByAccountIdFavoriteMoviesData, Get3AccountByAccountIdFavoriteMoviesResponse, Get3AccountByAccountIdFavoriteTvData, Get3AccountByAccountIdFavoriteTvResponse, Get3AccountByAccountIdListsData, Get3AccountByAccountIdListsResponse, Get3AccountByAccountIdRatedMoviesData, Get3AccountByAccountIdRatedMoviesResponse, Get3AccountByAccountIdRatedTvData, Get3AccountByAccountIdRatedTvEpisodesData, Get3AccountByAccountIdRatedTvEpisodesResponse, Get3AccountByAccountIdRatedTvResponse, Get3AccountByAccountIdResponse, Get3AccountByAccountIdWatchlistMoviesData, Get3AccountByAccountIdWatchlistMoviesResponse, Get3AccountByAccountIdWatchlistTvData, Get3AccountByAccountIdWatchlistTvResponse, Get3AuthenticationErrors, Get3AuthenticationGuestSessionNewResponse, Get3AuthenticationResponses, Get3AuthenticationTokenNewResponse, Get3CertificationMovieListResponse, Get3CertificationTvListResponse, Get3CollectionByCollectionIdData, Get3CollectionByCollectionIdImagesData, Get3CollectionByCollectionIdImagesResponse, Get3CollectionByCollectionIdResponse, Get3CollectionByCollectionIdTranslationsData, Get3CollectionByCollectionIdTranslationsResponse, Get3CompanyByCompanyIdAlternativeNamesData, Get3CompanyByCompanyIdAlternativeNamesResponse, Get3CompanyByCompanyIdData, Get3CompanyByCompanyIdImagesData, Get3CompanyByCompanyIdImagesResponse, Get3CompanyByCompanyIdResponse, Get3ConfigurationCountriesData, Get3ConfigurationCountriesResponse, Get3ConfigurationJobsResponse, Get3ConfigurationLanguagesResponse, Get3ConfigurationPrimaryTranslationsResponse, Get3ConfigurationResponse, Get3ConfigurationTimezonesResponse, Get3CreditByCreditIdData, Get3CreditByCreditIdResponse, Get3DiscoverMovieData, Get3DiscoverMovieResponse, Get3DiscoverTvData, Get3DiscoverTvResponse, Get3FindByExternalIdData, Get3FindByExternalIdResponse, Get3GenreMovieListData, Get3GenreMovieListResponse, Get3GenreTvListData, Get3GenreTvListResponse, Get3GuestSessionByGuestSessionIdRatedMoviesData, Get3GuestSessionByGuestSessionIdRatedMoviesResponse, Get3GuestSessionByGuestSessionIdRatedTvData, Get3GuestSessionByGuestSessionIdRatedTvEpisodesData, Get3GuestSessionByGuestSessionIdRatedTvEpisodesResponse, Get3GuestSessionByGuestSessionIdRatedTvResponse, Get3KeywordByKeywordIdData, Get3KeywordByKeywordIdMoviesData, Get3KeywordByKeywordIdMoviesResponse, Get3KeywordByKeywordIdResponse, Get3ListByListIdData, Get3ListByListIdItemStatusData, Get3ListByListIdItemStatusResponse, Get3ListByListIdResponse, Get3MovieByMovieIdAccountStatesData, Get3MovieByMovieIdAccountStatesResponse, Get3MovieByMovieIdAlternativeTitlesData, Get3MovieByMovieIdAlternativeTitlesResponse, Get3MovieByMovieIdChangesData, Get3MovieByMovieIdChangesResponse, Get3MovieByMovieIdCreditsData, Get3MovieByMovieIdCreditsResponse, Get3MovieByMovieIdData, Get3MovieByMovieIdExternalIdsData, Get3MovieByMovieIdExternalIdsResponse, Get3MovieByMovieIdImagesData, Get3MovieByMovieIdImagesResponse, Get3MovieByMovieIdKeywordsData, Get3MovieByMovieIdKeywordsResponse, Get3MovieByMovieIdListsData, Get3MovieByMovieIdListsResponse, Get3MovieByMovieIdRecommendationsData, Get3MovieByMovieIdRecommendationsResponse, Get3MovieByMovieIdReleaseDatesData, Get3MovieByMovieIdReleaseDatesResponse, Get3MovieByMovieIdResponse, Get3MovieByMovieIdReviewsData, Get3MovieByMovieIdReviewsResponse, Get3MovieByMovieIdSimilarData, Get3MovieByMovieIdSimilarResponse, Get3MovieByMovieIdTranslationsData, Get3MovieByMovieIdTranslationsResponse, Get3MovieByMovieIdVideosData, Get3MovieByMovieIdVideosResponse, Get3MovieByMovieIdWatchProvidersData, Get3MovieByMovieIdWatchProvidersResponse, Get3MovieChangesData, Get3MovieChangesResponse, Get3MovieLatestResponse, Get3MovieNowPlayingData, Get3MovieNowPlayingResponse, Get3MoviePopularData, Get3MoviePopularResponse, Get3MovieTopRatedData, Get3MovieTopRatedResponse, Get3MovieUpcomingData, Get3MovieUpcomingResponse, Get3NetworkByNetworkIdAlternativeNamesData, Get3NetworkByNetworkIdAlternativeNamesResponse, Get3NetworkByNetworkIdData, Get3NetworkByNetworkIdImagesData, Get3NetworkByNetworkIdImagesResponse, Get3NetworkByNetworkIdResponse, Get3PersonByPersonIdChangesData, Get3PersonByPersonIdChangesResponse, Get3PersonByPersonIdCombinedCreditsData, Get3PersonByPersonIdCombinedCreditsResponse, Get3PersonByPersonIdData, Get3PersonByPersonIdExternalIdsData, Get3PersonByPersonIdExternalIdsResponse, Get3PersonByPersonIdImagesData, Get3PersonByPersonIdImagesResponse, Get3PersonByPersonIdMovieCreditsData, Get3PersonByPersonIdMovieCreditsResponse, Get3PersonByPersonIdResponse, Get3PersonByPersonIdTaggedImagesData, Get3PersonByPersonIdTaggedImagesResponse, Get3PersonByPersonIdTranslationsData, Get3PersonByPersonIdTranslationsResponse, Get3PersonByPersonIdTvCreditsData, Get3PersonByPersonIdTvCreditsResponse, Get3PersonChangesData, Get3PersonChangesResponse, Get3PersonLatestResponse, Get3PersonPopularData, Get3PersonPopularResponse, Get3ReviewByReviewIdData, Get3ReviewByReviewIdResponse, Get3SearchCollectionData, Get3SearchCollectionResponse, Get3SearchCompanyData, Get3SearchCompanyResponse, Get3SearchKeywordData, Get3SearchKeywordResponse, Get3SearchMovieData, Get3SearchMovieResponse, Get3SearchMultiData, Get3SearchMultiResponse, Get3SearchPersonData, Get3SearchPersonResponse, Get3SearchTvData, Get3SearchTvResponse, Get3TrendingAllByTimeWindowData, Get3TrendingAllByTimeWindowResponse, Get3TrendingMovieByTimeWindowData, Get3TrendingMovieByTimeWindowResponse, Get3TrendingPersonByTimeWindowData, Get3TrendingPersonByTimeWindowResponse, Get3TrendingTvByTimeWindowData, Get3TrendingTvByTimeWindowResponse, Get3TvAiringTodayData, Get3TvAiringTodayResponse, Get3TvBySeriesIdAccountStatesData, Get3TvBySeriesIdAccountStatesResponse, Get3TvBySeriesIdAggregateCreditsData, Get3TvBySeriesIdAggregateCreditsResponse, Get3TvBySeriesIdAlternativeTitlesData, Get3TvBySeriesIdAlternativeTitlesResponse, Get3TvBySeriesIdChangesData, Get3TvBySeriesIdChangesResponse, Get3TvBySeriesIdContentRatingsData, Get3TvBySeriesIdContentRatingsResponse, Get3TvBySeriesIdCreditsData, Get3TvBySeriesIdCreditsResponse, Get3TvBySeriesIdData, Get3TvBySeriesIdEpisodeGroupsData, Get3TvBySeriesIdEpisodeGroupsResponse, Get3TvBySeriesIdExternalIdsData, Get3TvBySeriesIdExternalIdsResponse, Get3TvBySeriesIdImagesData, Get3TvBySeriesIdImagesResponse, Get3TvBySeriesIdKeywordsData, Get3TvBySeriesIdKeywordsResponse, Get3TvBySeriesIdListsData, Get3TvBySeriesIdListsResponse, Get3TvBySeriesIdRecommendationsData, Get3TvBySeriesIdRecommendationsResponse, Get3TvBySeriesIdResponse, Get3TvBySeriesIdReviewsData, Get3TvBySeriesIdReviewsResponse, Get3TvBySeriesIdScreenedTheatricallyData, Get3TvBySeriesIdScreenedTheatricallyResponse, Get3TvBySeriesIdSeasonBySeasonNumberAccountStatesData, Get3TvBySeriesIdSeasonBySeasonNumberAccountStatesResponse, Get3TvBySeriesIdSeasonBySeasonNumberAggregateCreditsData, Get3TvBySeriesIdSeasonBySeasonNumberAggregateCreditsResponse, Get3TvBySeriesIdSeasonBySeasonNumberCreditsData, Get3TvBySeriesIdSeasonBySeasonNumberCreditsResponse, Get3TvBySeriesIdSeasonBySeasonNumberData, Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberAccountStatesData, Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberAccountStatesResponse, Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberCreditsData, Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberCreditsResponse, Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberData, Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberExternalIdsData, Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberExternalIdsResponse, Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberImagesData, Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberImagesResponse, Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberResponse, Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberTranslationsData, Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberTranslationsResponse, Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberVideosData, Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberVideosResponse, Get3TvBySeriesIdSeasonBySeasonNumberExternalIdsData, Get3TvBySeriesIdSeasonBySeasonNumberExternalIdsResponse, Get3TvBySeriesIdSeasonBySeasonNumberImagesData, Get3TvBySeriesIdSeasonBySeasonNumberImagesResponse, Get3TvBySeriesIdSeasonBySeasonNumberResponse, Get3TvBySeriesIdSeasonBySeasonNumberTranslationsData, Get3TvBySeriesIdSeasonBySeasonNumberTranslationsResponse, Get3TvBySeriesIdSeasonBySeasonNumberVideosData, Get3TvBySeriesIdSeasonBySeasonNumberVideosResponse, Get3TvBySeriesIdSeasonBySeasonNumberWatchProvidersData, Get3TvBySeriesIdSeasonBySeasonNumberWatchProvidersResponse, Get3TvBySeriesIdSimilarData, Get3TvBySeriesIdSimilarResponse, Get3TvBySeriesIdTranslationsData, Get3TvBySeriesIdTranslationsResponse, Get3TvBySeriesIdVideosData, Get3TvBySeriesIdVideosResponse, Get3TvBySeriesIdWatchProvidersData, Get3TvBySeriesIdWatchProvidersResponse, Get3TvChangesData, Get3TvChangesResponse, Get3TvEpisodeByEpisodeIdChangesData, Get3TvEpisodeByEpisodeIdChangesResponse, Get3TvEpisodeGroupByTvEpisodeGroupIdData, Get3TvEpisodeGroupByTvEpisodeGroupIdResponse, Get3TvLatestResponse, Get3TvOnTheAirData, Get3TvOnTheAirResponse, Get3TvPopularData, Get3TvPopularResponse, Get3TvSeasonBySeasonIdChangesData, Get3TvSeasonBySeasonIdChangesResponse, Get3TvTopRatedData, Get3TvTopRatedResponse, Get3WatchProvidersMovieData, Get3WatchProvidersMovieResponse, Get3WatchProvidersRegionsData, Get3WatchProvidersRegionsResponse, Get3WatchProvidersTvData, Get3WatchProvidersTvResponse, Post3AccountByAccountIdFavoriteData, Post3AccountByAccountIdFavoriteResponse, Post3AccountByAccountIdWatchlistData, Post3AccountByAccountIdWatchlistResponse, Post3AuthenticationSessionConvert4Data, Post3AuthenticationSessionConvert4Response, Post3AuthenticationSessionNewData, Post3AuthenticationSessionNewResponse, Post3AuthenticationTokenValidateWithLoginData, Post3AuthenticationTokenValidateWithLoginResponse, Post3ListByListIdAddItemData, Post3ListByListIdAddItemResponse, Post3ListByListIdClearData, Post3ListByListIdClearResponse, Post3ListByListIdRemoveItemData, Post3ListByListIdRemoveItemResponse, Post3ListData, Post3ListResponse, Post3MovieByMovieIdRatingData, Post3MovieByMovieIdRatingResponse, Post3TvBySeriesIdRatingData, Post3TvBySeriesIdRatingResponse, Post3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberRatingData, Post3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberRatingResponse } from '../types.gen.js';

export type Options = {
    faker?: Faker;
    /**
     * Whether to include optional properties, including add property to record object.
     * Provide a number between 0 and 1 to randomly include based on that probability.
     * @default true
     */
    includeOptional?: boolean | number;
    /**
     * Whether to use schema default values instead of generating fake data.
     * Provide a number between 0 and 1 to randomly use defaults based on that probability.
     * @default false
     */
    useDefault?: boolean | number;
};

const resolveCondition = (condition: boolean | number, faker: Faker): boolean => condition === true || typeof condition === 'number' && faker.datatype.boolean({ probability: condition });

export const fakeGet3AuthenticationResponse200 = (options?: Options): Get3AuthenticationResponses[200] => {
    const f = options?.faker ?? faker;
    return {
        success: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
        status_code: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        status_message: f.string.sample()
    };
};

export const fakeGet3AuthenticationResponse401 = (options?: Options): Get3AuthenticationErrors[401] => {
    const f = options?.faker ?? faker;
    return {
        status_code: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        status_message: f.string.sample(),
        success: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean()
    };
};

export const fakeGet3AccountByAccountIdRequest = (options?: Options): Omit<Get3AccountByAccountIdData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            account_id: resolveCondition(options?.useDefault ?? false, f) ? null : f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { session_id: f.string.sample() }
        }
    };
};

export const fakeGet3AccountByAccountIdResponse = (options?: Options): Get3AccountByAccountIdResponse => {
    const f = options?.faker ?? faker;
    return {
        avatar: {
            gravatar: {
                hash: f.string.sample()
            },
            tmdb: {
                avatar_path: f.string.sample()
            }
        },
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        iso_639_1: f.string.sample(),
        iso_3166_1: f.string.sample(),
        name: f.string.sample(),
        include_adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
        username: f.internet.username()
    };
};

export const fakePost3AccountByAccountIdFavoriteRequest = (options?: Options): Omit<Post3AccountByAccountIdFavoriteData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        body: {
            RAW_BODY: f.string.sample()
        },
        path: {
            account_id: resolveCondition(options?.useDefault ?? false, f) ? null : f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { session_id: f.string.sample() }
        }
    };
};

export const fakePost3AccountByAccountIdFavoriteResponse = (options?: Options): Post3AccountByAccountIdFavoriteResponse => {
    const f = options?.faker ?? faker;
    return {
        status_code: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        status_message: f.string.sample()
    };
};

export const fakePost3AccountByAccountIdWatchlistRequest = (options?: Options): Omit<Post3AccountByAccountIdWatchlistData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        body: {
            RAW_BODY: f.string.sample()
        },
        path: {
            account_id: resolveCondition(options?.useDefault ?? false, f) ? null : f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { session_id: f.string.sample() }
        }
    };
};

export const fakePost3AccountByAccountIdWatchlistResponse = (options?: Options): Post3AccountByAccountIdWatchlistResponse => {
    const f = options?.faker ?? faker;
    return {
        status_code: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        status_message: f.string.sample()
    };
};

export const fakeGet3AccountByAccountIdFavoriteMoviesRequest = (options?: Options): Omit<Get3AccountByAccountIdFavoriteMoviesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            account_id: resolveCondition(options?.useDefault ?? false, f) ? null : f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { session_id: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { sort_by: resolveCondition(options?.useDefault ?? false, f) ? 'created_at.asc' : f.helpers.arrayElement(['created_at.asc', 'created_at.desc']) }
        }
    };
};

export const fakeGet3AccountByAccountIdFavoriteMoviesResponse = (options?: Options): Get3AccountByAccountIdFavoriteMoviesResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            original_language: f.string.sample(),
            original_title: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            release_date: f.string.sample(),
            title: f.lorem.words(),
            video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3AccountByAccountIdFavoriteTvRequest = (options?: Options): Omit<Get3AccountByAccountIdFavoriteTvData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            account_id: resolveCondition(options?.useDefault ?? false, f) ? null : f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { session_id: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { sort_by: resolveCondition(options?.useDefault ?? false, f) ? 'created_at.asc' : f.helpers.arrayElement(['created_at.asc', 'created_at.desc']) }
        }
    };
};

export const fakeGet3AccountByAccountIdFavoriteTvResponse = (options?: Options): Get3AccountByAccountIdFavoriteTvResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            origin_country: f.helpers.multiple(() => f.string.sample()),
            original_language: f.string.sample(),
            original_name: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            first_air_date: f.string.sample(),
            name: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3AccountByAccountIdListsRequest = (options?: Options): Omit<Get3AccountByAccountIdListsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            account_id: resolveCondition(options?.useDefault ?? false, f) ? null : f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { session_id: f.string.sample() }
        }
    };
};

export const fakeGet3AccountByAccountIdListsResponse = (options?: Options): Get3AccountByAccountIdListsResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            description: f.lorem.sentence(),
            favorite_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            item_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            iso_639_1: f.string.sample(),
            list_type: f.string.sample(),
            name: f.string.sample(),
            poster_path: undefined
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3AccountByAccountIdRatedMoviesRequest = (options?: Options): Omit<Get3AccountByAccountIdRatedMoviesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            account_id: resolveCondition(options?.useDefault ?? false, f) ? null : f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { session_id: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { sort_by: resolveCondition(options?.useDefault ?? false, f) ? 'created_at.asc' : f.helpers.arrayElement(['created_at.asc', 'created_at.desc']) }
        }
    };
};

export const fakeGet3AccountByAccountIdRatedMoviesResponse = (options?: Options): Get3AccountByAccountIdRatedMoviesResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            original_language: f.string.sample(),
            original_title: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            release_date: f.string.sample(),
            title: f.lorem.words(),
            video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            rating: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3AccountByAccountIdRatedTvRequest = (options?: Options): Omit<Get3AccountByAccountIdRatedTvData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            account_id: resolveCondition(options?.useDefault ?? false, f) ? null : f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { session_id: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { sort_by: resolveCondition(options?.useDefault ?? false, f) ? 'created_at.asc' : f.helpers.arrayElement(['created_at.asc', 'created_at.desc']) }
        }
    };
};

export const fakeGet3AccountByAccountIdRatedTvResponse = (options?: Options): Get3AccountByAccountIdRatedTvResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            origin_country: f.helpers.multiple(() => f.string.sample()),
            original_language: f.string.sample(),
            original_name: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            first_air_date: f.string.sample(),
            name: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            rating: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3AccountByAccountIdRatedTvEpisodesRequest = (options?: Options): Omit<Get3AccountByAccountIdRatedTvEpisodesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            account_id: resolveCondition(options?.useDefault ?? false, f) ? null : f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { session_id: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { sort_by: resolveCondition(options?.useDefault ?? false, f) ? 'created_at.asc' : f.helpers.arrayElement(['created_at.asc', 'created_at.desc']) }
        }
    };
};

export const fakeGet3AccountByAccountIdRatedTvEpisodesResponse = (options?: Options): Get3AccountByAccountIdRatedTvEpisodesResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            air_date: f.string.sample(),
            episode_number: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample(),
            overview: f.string.sample(),
            production_code: f.string.sample(),
            runtime: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            season_number: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            show_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            still_path: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            rating: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3AccountByAccountIdWatchlistMoviesRequest = (options?: Options): Omit<Get3AccountByAccountIdWatchlistMoviesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            account_id: resolveCondition(options?.useDefault ?? false, f) ? null : f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { session_id: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { sort_by: resolveCondition(options?.useDefault ?? false, f) ? 'created_at.asc' : f.helpers.arrayElement(['created_at.asc', 'created_at.desc']) }
        }
    };
};

export const fakeGet3AccountByAccountIdWatchlistMoviesResponse = (options?: Options): Get3AccountByAccountIdWatchlistMoviesResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            original_language: f.string.sample(),
            original_title: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            release_date: f.string.sample(),
            title: f.lorem.words(),
            video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3AccountByAccountIdWatchlistTvRequest = (options?: Options): Omit<Get3AccountByAccountIdWatchlistTvData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            account_id: resolveCondition(options?.useDefault ?? false, f) ? null : f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { session_id: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { sort_by: resolveCondition(options?.useDefault ?? false, f) ? 'created_at.asc' : f.helpers.arrayElement(['created_at.asc', 'created_at.desc']) }
        }
    };
};

export const fakeGet3AccountByAccountIdWatchlistTvResponse = (options?: Options): Get3AccountByAccountIdWatchlistTvResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            origin_country: f.helpers.multiple(() => f.string.sample()),
            original_language: f.string.sample(),
            original_name: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            first_air_date: f.string.sample(),
            name: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3AuthenticationGuestSessionNewResponse = (options?: Options): Get3AuthenticationGuestSessionNewResponse => {
    const f = options?.faker ?? faker;
    return {
        success: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
        guest_session_id: f.string.sample(),
        expires_at: f.string.sample()
    };
};

export const fakeGet3AuthenticationTokenNewResponse = (options?: Options): Get3AuthenticationTokenNewResponse => {
    const f = options?.faker ?? faker;
    return {
        success: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
        expires_at: f.string.sample(),
        request_token: f.string.sample()
    };
};

export const fakePost3AuthenticationSessionNewRequest = (options?: Options): Omit<Post3AuthenticationSessionNewData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        body: {
            RAW_BODY: f.string.sample()
        }
    };
};

export const fakePost3AuthenticationSessionNewResponse = (options?: Options): Post3AuthenticationSessionNewResponse => {
    const f = options?.faker ?? faker;
    return {
        success: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
        session_id: f.string.sample()
    };
};

export const fakePost3AuthenticationSessionConvert4Request = (options?: Options): Omit<Post3AuthenticationSessionConvert4Data, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        body: {
            RAW_BODY: f.string.sample()
        }
    };
};

export const fakePost3AuthenticationSessionConvert4Response = (options?: Options): Post3AuthenticationSessionConvert4Response => {
    const f = options?.faker ?? faker;
    return {
        success: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
        session_id: f.string.sample()
    };
};

export const fakePost3AuthenticationTokenValidateWithLoginRequest = (options?: Options): Omit<Post3AuthenticationTokenValidateWithLoginData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        body: {
            RAW_BODY: f.string.sample()
        }
    };
};

export const fakePost3AuthenticationTokenValidateWithLoginResponse = (options?: Options): Post3AuthenticationTokenValidateWithLoginResponse => {
    const f = options?.faker ?? faker;
    return {
        success: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
        expires_at: f.string.sample(),
        request_token: f.string.sample()
    };
};

export const fakeDelete3AuthenticationSessionRequest = (options?: Options): Omit<Delete3AuthenticationSessionData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        body: {
            RAW_BODY: f.string.sample()
        }
    };
};

export const fakeDelete3AuthenticationSessionResponse = (options?: Options): Delete3AuthenticationSessionResponse => {
    const f = options?.faker ?? faker;
    return {
        success: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean()
    };
};

export const fakeGet3CertificationMovieListResponse = (options?: Options): Get3CertificationMovieListResponse => {
    const f = options?.faker ?? faker;
    return {
        certifications: {
            AU: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            BG: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            BR: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            CA: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            'CA-QC': f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            DE: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            DK: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            ES: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            FI: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            FR: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            GB: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            HU: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            IN: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            IT: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            LT: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            MY: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            NL: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            NO: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            NZ: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            PH: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            PT: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            RU: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            SE: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            US: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            KR: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            SK: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            TH: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            MX: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            ID: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            TR: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            AR: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            GR: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            TW: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            ZA: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            SG: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            IE: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            PR: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            JP: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            VI: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            CH: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            IL: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            HK: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            MO: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            LV: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            LU: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            }))
        }
    };
};

export const fakeGet3CertificationTvListResponse = (options?: Options): Get3CertificationTvListResponse => {
    const f = options?.faker ?? faker;
    return {
        certifications: {
            AU: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            BR: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            CA: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            'CA-QC': f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            DE: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            ES: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            FR: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            GB: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            HU: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            KR: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            LT: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            NL: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            PH: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            PT: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            RU: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            SK: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            TH: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            US: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            IT: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            FI: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            MY: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            NZ: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            NO: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            BG: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            MX: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            IN: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            DK: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            SE: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            ID: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            TR: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            AR: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            PL: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            MA: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            GR: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            IL: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            TW: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            ZA: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            SG: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            PR: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            VI: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                meaning: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            }))
        }
    };
};

export const fakeGet3MovieChangesRequest = (options?: Options): Omit<Get3MovieChangesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { end_date: f.date.recent().toISOString().slice(0, 10) },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { start_date: f.date.recent().toISOString().slice(0, 10) }
        }
    };
};

export const fakeGet3MovieChangesResponse = (options?: Options): Get3MovieChangesResponse => {
    const f = options?.faker ?? faker;
    return {
        results: f.helpers.multiple(() => ({
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean()
        })),
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3PersonChangesRequest = (options?: Options): Omit<Get3PersonChangesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { end_date: f.date.recent().toISOString().slice(0, 10) },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { start_date: f.date.recent().toISOString().slice(0, 10) }
        }
    };
};

export const fakeGet3PersonChangesResponse = (options?: Options): Get3PersonChangesResponse => {
    const f = options?.faker ?? faker;
    return {
        results: f.helpers.multiple(() => ({
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean()
        })),
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TvChangesRequest = (options?: Options): Omit<Get3TvChangesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { end_date: f.date.recent().toISOString().slice(0, 10) },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { start_date: f.date.recent().toISOString().slice(0, 10) }
        }
    };
};

export const fakeGet3TvChangesResponse = (options?: Options): Get3TvChangesResponse => {
    const f = options?.faker ?? faker;
    return {
        results: f.helpers.multiple(() => ({
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean()
        })),
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3CollectionByCollectionIdRequest = (options?: Options): Omit<Get3CollectionByCollectionIdData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            collection_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3CollectionByCollectionIdResponse = (options?: Options): Get3CollectionByCollectionIdResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        name: f.string.sample(),
        original_language: f.string.sample(),
        original_name: f.string.sample(),
        overview: f.string.sample(),
        poster_path: f.string.sample(),
        backdrop_path: f.string.sample(),
        parts: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample(),
            original_name: f.string.sample(),
            overview: f.string.sample(),
            poster_path: f.string.sample(),
            media_type: f.string.sample(),
            original_language: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            release_date: f.string.sample(),
            video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        }))
    };
};

export const fakeGet3CollectionByCollectionIdImagesRequest = (options?: Options): Omit<Get3CollectionByCollectionIdImagesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            collection_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { include_image_language: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: f.string.sample() }
        }
    };
};

export const fakeGet3CollectionByCollectionIdImagesResponse = (options?: Options): Get3CollectionByCollectionIdImagesResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        backdrops: f.helpers.multiple(() => ({
            aspect_ratio: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            height: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            iso_639_1: undefined,
            file_path: f.system.filePath(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            width: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        posters: f.helpers.multiple(() => ({
            aspect_ratio: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            height: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            iso_639_1: f.string.sample(),
            file_path: f.system.filePath(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            width: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        }))
    };
};

export const fakeGet3CollectionByCollectionIdTranslationsRequest = (options?: Options): Omit<Get3CollectionByCollectionIdTranslationsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            collection_id: f.number.int()
        }
    };
};

export const fakeGet3CollectionByCollectionIdTranslationsResponse = (options?: Options): Get3CollectionByCollectionIdTranslationsResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        translations: f.helpers.multiple(() => ({
            iso_3166_1: f.string.sample(),
            iso_639_1: f.string.sample(),
            name: f.string.sample(),
            english_name: f.string.sample(),
            data: {
                title: f.lorem.words(),
                overview: f.string.sample(),
                homepage: f.internet.url()
            }
        }))
    };
};

export const fakeGet3CompanyByCompanyIdRequest = (options?: Options): Omit<Get3CompanyByCompanyIdData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            company_id: f.number.int()
        }
    };
};

export const fakeGet3CompanyByCompanyIdResponse = (options?: Options): Get3CompanyByCompanyIdResponse => {
    const f = options?.faker ?? faker;
    return {
        description: f.lorem.sentence(),
        headquarters: f.string.sample(),
        homepage: f.internet.url(),
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        logo_path: f.string.sample(),
        name: f.string.sample(),
        origin_country: f.string.sample(),
        parent_company: undefined
    };
};

export const fakeGet3CompanyByCompanyIdAlternativeNamesRequest = (options?: Options): Omit<Get3CompanyByCompanyIdAlternativeNamesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            company_id: f.number.int()
        }
    };
};

export const fakeGet3CompanyByCompanyIdAlternativeNamesResponse = (options?: Options): Get3CompanyByCompanyIdAlternativeNamesResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            name: f.string.sample(),
            type: f.string.sample()
        }))
    };
};

export const fakeGet3CompanyByCompanyIdImagesRequest = (options?: Options): Omit<Get3CompanyByCompanyIdImagesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            company_id: f.number.int()
        }
    };
};

export const fakeGet3CompanyByCompanyIdImagesResponse = (options?: Options): Get3CompanyByCompanyIdImagesResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        logos: f.helpers.multiple(() => ({
            aspect_ratio: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            file_path: f.system.filePath(),
            height: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: f.string.uuid(),
            file_type: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            width: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        }))
    };
};

export const fakeGet3ConfigurationResponse = (options?: Options): Get3ConfigurationResponse => {
    const f = options?.faker ?? faker;
    return {
        images: {
            base_url: f.string.sample(),
            secure_base_url: f.string.sample(),
            backdrop_sizes: f.helpers.multiple(() => f.string.sample()),
            logo_sizes: f.helpers.multiple(() => f.string.sample()),
            poster_sizes: f.helpers.multiple(() => f.string.sample()),
            profile_sizes: f.helpers.multiple(() => f.string.sample()),
            still_sizes: f.helpers.multiple(() => f.string.sample())
        },
        change_keys: f.helpers.multiple(() => f.string.sample())
    };
};

export const fakeGet3ConfigurationCountriesRequest = (options?: Options): Omit<Get3ConfigurationCountriesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3ConfigurationCountriesResponse = (options?: Options): Get3ConfigurationCountriesResponse => {
    const f = options?.faker ?? faker;
    return f.helpers.multiple(() => ({
        iso_3166_1: f.string.sample(),
        english_name: f.string.sample(),
        native_name: f.string.sample()
    }));
};

export const fakeGet3ConfigurationJobsResponse = (options?: Options): Get3ConfigurationJobsResponse => {
    const f = options?.faker ?? faker;
    return f.helpers.multiple(() => ({
        department: f.string.sample(),
        jobs: f.helpers.multiple(() => f.string.sample())
    }));
};

export const fakeGet3ConfigurationLanguagesResponse = (options?: Options): Get3ConfigurationLanguagesResponse => {
    const f = options?.faker ?? faker;
    return f.helpers.multiple(() => ({
        iso_639_1: f.string.sample(),
        english_name: f.string.sample(),
        name: f.string.sample()
    }));
};

export const fakeGet3ConfigurationPrimaryTranslationsResponse = (options?: Options): Get3ConfigurationPrimaryTranslationsResponse => {
    const f = options?.faker ?? faker;
    return f.helpers.multiple(() => f.string.sample());
};

export const fakeGet3ConfigurationTimezonesResponse = (options?: Options): Get3ConfigurationTimezonesResponse => {
    const f = options?.faker ?? faker;
    return f.helpers.multiple(() => ({
        iso_3166_1: f.string.sample(),
        zones: f.helpers.multiple(() => f.string.sample())
    }));
};

export const fakeGet3CreditByCreditIdRequest = (options?: Options): Omit<Get3CreditByCreditIdData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            credit_id: f.string.sample()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3CreditByCreditIdResponse = (options?: Options): Get3CreditByCreditIdResponse => {
    const f = options?.faker ?? faker;
    return {
        credit_type: f.string.sample(),
        department: f.string.sample(),
        job: f.string.sample(),
        media: {
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample(),
            original_language: f.string.sample(),
            original_name: f.string.sample(),
            overview: f.string.sample(),
            poster_path: f.string.sample(),
            media_type: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            first_air_date: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            origin_country: f.helpers.multiple(() => f.string.sample()),
            character: f.string.sample(),
            episodes: f.helpers.multiple(() => undefined),
            seasons: f.helpers.multiple(() => ({
                air_date: f.string.sample(),
                episode_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                name: f.string.sample(),
                overview: f.string.sample(),
                poster_path: f.string.sample(),
                season_number: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                show_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            }))
        },
        media_type: f.string.sample(),
        id: f.string.uuid(),
        person: {
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.person.fullName(),
            original_name: f.string.sample(),
            media_type: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            known_for_department: f.string.sample(),
            profile_path: f.string.sample()
        }
    };
};

export const fakeGet3DiscoverMovieRequest = (options?: Options): Omit<Get3DiscoverMovieData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { certification: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'certification.gte': f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'certification.lte': f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { certification_country: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { include_adult: resolveCondition(options?.useDefault ?? false, f) ? false : f.datatype.boolean() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { include_video: resolveCondition(options?.useDefault ?? false, f) ? false : f.datatype.boolean() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { primary_release_year: f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'primary_release_date.gte': f.date.recent().toISOString().slice(0, 10) },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'primary_release_date.lte': f.date.recent().toISOString().slice(0, 10) },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { region: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'release_date.gte': f.date.recent().toISOString().slice(0, 10) },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'release_date.lte': f.date.recent().toISOString().slice(0, 10) },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { sort_by: resolveCondition(options?.useDefault ?? false, f) ? 'popularity.desc' : f.helpers.arrayElement([
                    'original_title.asc',
                    'original_title.desc',
                    'popularity.asc',
                    'popularity.desc',
                    'revenue.asc',
                    'revenue.desc',
                    'primary_release_date.asc',
                    'title.asc',
                    'title.desc',
                    'primary_release_date.desc',
                    'vote_average.asc',
                    'vote_average.desc',
                    'vote_count.asc',
                    'vote_count.desc'
                ]) },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'vote_average.gte': f.number.float() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'vote_average.lte': f.number.float() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'vote_count.gte': f.number.float() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'vote_count.lte': f.number.float() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { watch_region: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { with_cast: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { with_companies: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { with_crew: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { with_genres: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { with_keywords: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { with_origin_country: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { with_original_language: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { with_people: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { with_release_type: f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'with_runtime.gte': f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'with_runtime.lte': f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { with_watch_monetization_types: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { with_watch_providers: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { without_companies: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { without_genres: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { without_keywords: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { without_watch_providers: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { year: f.number.int() }
        }
    };
};

export const fakeGet3DiscoverMovieResponse = (options?: Options): Get3DiscoverMovieResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            original_language: f.string.sample(),
            original_title: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            release_date: f.string.sample(),
            title: f.lorem.words(),
            video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3DiscoverTvRequest = (options?: Options): Omit<Get3DiscoverTvData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'air_date.gte': f.date.recent().toISOString().slice(0, 10) },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'air_date.lte': f.date.recent().toISOString().slice(0, 10) },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { first_air_date_year: f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'first_air_date.gte': f.date.recent().toISOString().slice(0, 10) },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'first_air_date.lte': f.date.recent().toISOString().slice(0, 10) },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { include_adult: resolveCondition(options?.useDefault ?? false, f) ? false : f.datatype.boolean() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { include_null_first_air_dates: resolveCondition(options?.useDefault ?? false, f) ? false : f.datatype.boolean() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { screened_theatrically: f.datatype.boolean() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { sort_by: resolveCondition(options?.useDefault ?? false, f) ? 'popularity.desc' : f.helpers.arrayElement([
                    'first_air_date.asc',
                    'first_air_date.desc',
                    'name.asc',
                    'name.desc',
                    'original_name.asc',
                    'original_name.desc',
                    'popularity.asc',
                    'popularity.desc',
                    'vote_average.asc',
                    'vote_average.desc',
                    'vote_count.asc',
                    'vote_count.desc'
                ]) },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { timezone: f.location.timeZone() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'vote_average.gte': f.number.float() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'vote_average.lte': f.number.float() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'vote_count.gte': f.number.float() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'vote_count.lte': f.number.float() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { watch_region: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { with_companies: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { with_genres: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { with_keywords: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { with_networks: f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { with_origin_country: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { with_original_language: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'with_runtime.gte': f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'with_runtime.lte': f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { with_status: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { with_watch_monetization_types: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { with_watch_providers: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { without_companies: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { without_genres: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { without_keywords: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { without_watch_providers: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { with_type: f.string.sample() }
        }
    };
};

export const fakeGet3DiscoverTvResponse = (options?: Options): Get3DiscoverTvResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            backdrop_path: f.string.sample(),
            first_air_date: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample(),
            origin_country: f.helpers.multiple(() => f.string.sample()),
            original_language: f.string.sample(),
            original_name: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3FindByExternalIdRequest = (options?: Options): Omit<Get3FindByExternalIdData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            external_id: f.string.sample()
        },
        query: {
            external_source: f.helpers.arrayElement([
                'imdb_id',
                'facebook_id',
                'instagram_id',
                'tvdb_id',
                'tiktok_id',
                'twitter_id',
                'wikidata_id',
                'youtube_id'
            ]),
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3FindByExternalIdResponse = (options?: Options): Get3FindByExternalIdResponse => {
    const f = options?.faker ?? faker;
    return {
        movie_results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            title: f.lorem.words(),
            original_language: f.string.sample(),
            original_title: f.string.sample(),
            overview: f.string.sample(),
            poster_path: f.string.sample(),
            media_type: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            release_date: f.string.sample(),
            video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        person_results: f.helpers.multiple(() => undefined),
        tv_results: f.helpers.multiple(() => undefined),
        tv_episode_results: f.helpers.multiple(() => undefined),
        tv_season_results: f.helpers.multiple(() => undefined)
    };
};

export const fakeGet3GenreMovieListRequest = (options?: Options): Omit<Get3GenreMovieListData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en' : f.string.sample() }
        }
    };
};

export const fakeGet3GenreMovieListResponse = (options?: Options): Get3GenreMovieListResponse => {
    const f = options?.faker ?? faker;
    return {
        genres: f.helpers.multiple(() => ({
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample()
        }))
    };
};

export const fakeGet3GenreTvListRequest = (options?: Options): Omit<Get3GenreTvListData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en' : f.string.sample() }
        }
    };
};

export const fakeGet3GenreTvListResponse = (options?: Options): Get3GenreTvListResponse => {
    const f = options?.faker ?? faker;
    return {
        genres: f.helpers.multiple(() => ({
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample()
        }))
    };
};

export const fakeGet3GuestSessionByGuestSessionIdRatedMoviesRequest = (options?: Options): Omit<Get3GuestSessionByGuestSessionIdRatedMoviesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            guest_session_id: f.string.sample()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { sort_by: resolveCondition(options?.useDefault ?? false, f) ? 'created_at.asc' : f.helpers.arrayElement(['created_at.asc', 'created_at.desc']) }
        }
    };
};

export const fakeGet3GuestSessionByGuestSessionIdRatedMoviesResponse = (options?: Options): Get3GuestSessionByGuestSessionIdRatedMoviesResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            original_language: f.string.sample(),
            original_title: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            release_date: f.string.sample(),
            title: f.lorem.words(),
            video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            rating: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3GuestSessionByGuestSessionIdRatedTvRequest = (options?: Options): Omit<Get3GuestSessionByGuestSessionIdRatedTvData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            guest_session_id: f.string.sample()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { sort_by: resolveCondition(options?.useDefault ?? false, f) ? 'created_at.asc' : f.helpers.arrayElement(['created_at.asc', 'created_at.desc']) }
        }
    };
};

export const fakeGet3GuestSessionByGuestSessionIdRatedTvResponse = (options?: Options): Get3GuestSessionByGuestSessionIdRatedTvResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            origin_country: f.helpers.multiple(() => f.string.sample()),
            original_language: f.string.sample(),
            original_name: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            first_air_date: f.string.sample(),
            name: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            rating: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3GuestSessionByGuestSessionIdRatedTvEpisodesRequest = (options?: Options): Omit<Get3GuestSessionByGuestSessionIdRatedTvEpisodesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            guest_session_id: f.string.sample()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { sort_by: resolveCondition(options?.useDefault ?? false, f) ? 'created_at.asc' : f.helpers.arrayElement(['created_at.asc', 'created_at.desc']) }
        }
    };
};

export const fakeGet3GuestSessionByGuestSessionIdRatedTvEpisodesResponse = (options?: Options): Get3GuestSessionByGuestSessionIdRatedTvEpisodesResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            air_date: f.string.sample(),
            episode_number: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample(),
            overview: f.string.sample(),
            production_code: f.string.sample(),
            runtime: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            season_number: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            show_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            still_path: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            rating: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3KeywordByKeywordIdRequest = (options?: Options): Omit<Get3KeywordByKeywordIdData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            keyword_id: f.number.int()
        }
    };
};

export const fakeGet3KeywordByKeywordIdResponse = (options?: Options): Get3KeywordByKeywordIdResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        name: f.string.sample()
    };
};

export const fakeGet3KeywordByKeywordIdMoviesRequest = (options?: Options): Omit<Get3KeywordByKeywordIdMoviesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            keyword_id: f.string.sample()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { include_adult: resolveCondition(options?.useDefault ?? false, f) ? false : f.datatype.boolean() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() }
        }
    };
};

export const fakeGet3KeywordByKeywordIdMoviesResponse = (options?: Options): Get3KeywordByKeywordIdMoviesResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            original_language: f.string.sample(),
            original_title: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            release_date: f.string.sample(),
            title: f.lorem.words(),
            video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakePost3ListByListIdAddItemRequest = (options?: Options): Omit<Post3ListByListIdAddItemData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        body: {
            RAW_BODY: f.string.sample()
        },
        path: {
            list_id: f.number.int()
        },
        query: {
            session_id: f.string.sample()
        }
    };
};

export const fakePost3ListByListIdAddItemResponse = (options?: Options): Post3ListByListIdAddItemResponse => {
    const f = options?.faker ?? faker;
    return {
        status_code: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        status_message: f.string.sample()
    };
};

export const fakeGet3ListByListIdItemStatusRequest = (options?: Options): Omit<Get3ListByListIdItemStatusData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            list_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { movie_id: f.number.int() }
        }
    };
};

export const fakeGet3ListByListIdItemStatusResponse = (options?: Options): Get3ListByListIdItemStatusResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        item_present: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean()
    };
};

export const fakePost3ListByListIdClearRequest = (options?: Options): Omit<Post3ListByListIdClearData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            list_id: f.number.int()
        },
        query: {
            session_id: f.string.sample(),
            confirm: resolveCondition(options?.useDefault ?? false, f) ? false : f.datatype.boolean()
        }
    };
};

export const fakePost3ListByListIdClearResponse = (options?: Options): Post3ListByListIdClearResponse => {
    const f = options?.faker ?? faker;
    return {
        status_code: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        status_message: f.string.sample()
    };
};

export const fakePost3ListRequest = (options?: Options): Omit<Post3ListData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        body: {
            RAW_BODY: f.string.sample()
        },
        query: {
            session_id: f.string.sample()
        }
    };
};

export const fakePost3ListResponse = (options?: Options): Post3ListResponse => {
    const f = options?.faker ?? faker;
    return {
        status_message: f.string.sample(),
        success: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
        status_code: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        list_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeDelete3ListByListIdRequest = (options?: Options): Omit<Delete3ListByListIdData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            list_id: f.number.int()
        },
        query: {
            session_id: f.string.sample()
        }
    };
};

export const fakeDelete3ListByListIdResponse = (options?: Options): Delete3ListByListIdResponse => {
    const f = options?.faker ?? faker;
    return {
        status_code: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        status_message: f.string.sample()
    };
};

export const fakeGet3ListByListIdRequest = (options?: Options): Omit<Get3ListByListIdData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            list_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() }
        }
    };
};

export const fakeGet3ListByListIdResponse = (options?: Options): Get3ListByListIdResponse => {
    const f = options?.faker ?? faker;
    return {
        created_by: f.string.sample(),
        description: f.lorem.sentence(),
        favorite_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        id: f.string.uuid(),
        items: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            media_type: f.string.sample(),
            original_language: f.string.sample(),
            original_title: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            release_date: f.string.sample(),
            title: f.lorem.words(),
            video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        item_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        iso_639_1: f.string.sample(),
        name: f.string.sample(),
        poster_path: f.string.sample()
    };
};

export const fakePost3ListByListIdRemoveItemRequest = (options?: Options): Omit<Post3ListByListIdRemoveItemData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        body: {
            RAW_BODY: f.string.sample()
        },
        path: {
            list_id: f.number.int()
        },
        query: {
            session_id: f.string.sample()
        }
    };
};

export const fakePost3ListByListIdRemoveItemResponse = (options?: Options): Post3ListByListIdRemoveItemResponse => {
    const f = options?.faker ?? faker;
    return {
        status_code: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        status_message: f.string.sample()
    };
};

export const fakeGet3MovieNowPlayingRequest = (options?: Options): Omit<Get3MovieNowPlayingData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { region: f.string.sample() }
        }
    };
};

export const fakeGet3MovieNowPlayingResponse = (options?: Options): Get3MovieNowPlayingResponse => {
    const f = options?.faker ?? faker;
    return {
        dates: {
            maximum: f.string.sample(),
            minimum: f.string.sample()
        },
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            original_language: f.string.sample(),
            original_title: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            release_date: f.string.sample(),
            title: f.lorem.words(),
            video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3MoviePopularRequest = (options?: Options): Omit<Get3MoviePopularData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { region: f.string.sample() }
        }
    };
};

export const fakeGet3MoviePopularResponse = (options?: Options): Get3MoviePopularResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            original_language: f.string.sample(),
            original_title: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            release_date: f.string.sample(),
            title: f.lorem.words(),
            video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3MovieTopRatedRequest = (options?: Options): Omit<Get3MovieTopRatedData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { region: f.string.sample() }
        }
    };
};

export const fakeGet3MovieTopRatedResponse = (options?: Options): Get3MovieTopRatedResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            original_language: f.string.sample(),
            original_title: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            release_date: f.string.sample(),
            title: f.lorem.words(),
            video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3MovieUpcomingRequest = (options?: Options): Omit<Get3MovieUpcomingData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { region: f.string.sample() }
        }
    };
};

export const fakeGet3MovieUpcomingResponse = (options?: Options): Get3MovieUpcomingResponse => {
    const f = options?.faker ?? faker;
    return {
        dates: {
            maximum: f.string.sample(),
            minimum: f.string.sample()
        },
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            original_language: f.string.sample(),
            original_title: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            release_date: f.string.sample(),
            title: f.lorem.words(),
            video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3MovieByMovieIdRequest = (options?: Options): Omit<Get3MovieByMovieIdData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            movie_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { append_to_response: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3MovieByMovieIdResponse = (options?: Options): Get3MovieByMovieIdResponse => {
    const f = options?.faker ?? faker;
    return {
        adult: resolveCondition(options?.useDefault ?? false, f) ? 'false' : f.datatype.boolean(),
        backdrop_path: f.string.sample(),
        belongs_to_collection: {
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample(),
            poster_path: f.string.sample(),
            backdrop_path: f.string.sample()
        },
        budget: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        genres: f.helpers.multiple(() => ({
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample()
        })),
        homepage: f.internet.url(),
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        imdb_id: f.string.sample(),
        origin_country: f.helpers.multiple(() => f.string.sample()),
        original_language: f.string.sample(),
        original_title: f.string.sample(),
        overview: f.string.sample(),
        popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
        poster_path: f.string.sample(),
        production_companies: f.helpers.multiple(() => ({
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            logo_path: f.string.sample(),
            name: f.string.sample(),
            origin_country: f.string.sample()
        })),
        production_countries: f.helpers.multiple(() => ({
            iso_3166_1: f.string.sample(),
            name: f.string.sample()
        })),
        release_date: f.string.sample(),
        revenue: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        runtime: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        spoken_languages: f.helpers.multiple(() => ({
            english_name: f.string.sample(),
            iso_639_1: f.string.sample(),
            name: f.string.sample()
        })),
        status: f.string.sample(),
        tagline: f.string.sample(),
        title: f.lorem.words(),
        video: resolveCondition(options?.useDefault ?? false, f) ? 'false' : f.datatype.boolean(),
        vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
        vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3MovieByMovieIdAccountStatesRequest = (options?: Options): Omit<Get3MovieByMovieIdAccountStatesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            movie_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { session_id: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { guest_session_id: f.string.sample() }
        }
    };
};

export const fakeGet3MovieByMovieIdAccountStatesResponse = (options?: Options): Get3MovieByMovieIdAccountStatesResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        favorite: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
        rated: {
            value: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        },
        watchlist: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean()
    };
};

export const fakeGet3MovieByMovieIdAlternativeTitlesRequest = (options?: Options): Omit<Get3MovieByMovieIdAlternativeTitlesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            movie_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { country: f.location.country() }
        }
    };
};

export const fakeGet3MovieByMovieIdAlternativeTitlesResponse = (options?: Options): Get3MovieByMovieIdAlternativeTitlesResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        titles: f.helpers.multiple(() => ({
            iso_3166_1: f.string.sample(),
            title: f.lorem.words(),
            type: f.string.sample()
        }))
    };
};

export const fakeGet3MovieByMovieIdChangesRequest = (options?: Options): Omit<Get3MovieByMovieIdChangesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            movie_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { end_date: f.date.recent().toISOString().slice(0, 10) },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { start_date: f.date.recent().toISOString().slice(0, 10) }
        }
    };
};

export const fakeGet3MovieByMovieIdChangesResponse = (options?: Options): Get3MovieByMovieIdChangesResponse => {
    const f = options?.faker ?? faker;
    return {
        changes: f.helpers.multiple(() => ({
            key: f.string.sample(),
            items: f.helpers.multiple(() => ({
                id: f.string.uuid(),
                action: f.string.sample(),
                time: f.string.sample(),
                iso_639_1: f.string.sample(),
                iso_3166_1: f.string.sample(),
                value: {
                    poster: {
                        file_path: f.system.filePath()
                    }
                }
            }))
        }))
    };
};

export const fakeGet3MovieByMovieIdCreditsRequest = (options?: Options): Omit<Get3MovieByMovieIdCreditsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            movie_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3MovieByMovieIdCreditsResponse = (options?: Options): Get3MovieByMovieIdCreditsResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        cast: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            known_for_department: f.string.sample(),
            name: f.string.sample(),
            original_name: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            profile_path: f.string.sample(),
            cast_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            character: f.string.sample(),
            credit_id: f.string.sample(),
            order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        crew: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            known_for_department: f.string.sample(),
            name: f.string.sample(),
            original_name: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            profile_path: f.string.sample(),
            credit_id: f.string.sample(),
            department: f.string.sample(),
            job: f.string.sample()
        }))
    };
};

export const fakeGet3MovieByMovieIdExternalIdsRequest = (options?: Options): Omit<Get3MovieByMovieIdExternalIdsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            movie_id: f.number.int()
        }
    };
};

export const fakeGet3MovieByMovieIdExternalIdsResponse = (options?: Options): Get3MovieByMovieIdExternalIdsResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        imdb_id: f.string.sample(),
        wikidata_id: undefined,
        facebook_id: f.string.sample(),
        instagram_id: undefined,
        twitter_id: undefined
    };
};

export const fakeGet3MovieByMovieIdImagesRequest = (options?: Options): Omit<Get3MovieByMovieIdImagesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            movie_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { include_image_language: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: f.string.sample() }
        }
    };
};

export const fakeGet3MovieByMovieIdImagesResponse = (options?: Options): Get3MovieByMovieIdImagesResponse => {
    const f = options?.faker ?? faker;
    return {
        backdrops: f.helpers.multiple(() => ({
            aspect_ratio: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            height: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            iso_639_1: undefined,
            file_path: f.system.filePath(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            width: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        logos: f.helpers.multiple(() => ({
            aspect_ratio: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            height: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            iso_639_1: f.string.sample(),
            file_path: f.system.filePath(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            width: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        posters: f.helpers.multiple(() => ({
            aspect_ratio: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            height: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            iso_639_1: f.string.sample(),
            file_path: f.system.filePath(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            width: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        }))
    };
};

export const fakeGet3MovieByMovieIdKeywordsRequest = (options?: Options): Omit<Get3MovieByMovieIdKeywordsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            movie_id: f.string.sample()
        }
    };
};

export const fakeGet3MovieByMovieIdKeywordsResponse = (options?: Options): Get3MovieByMovieIdKeywordsResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        keywords: f.helpers.multiple(() => ({
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample()
        }))
    };
};

export const fakeGet3MovieLatestResponse = (options?: Options): Get3MovieLatestResponse => {
    const f = options?.faker ?? faker;
    return {
        adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
        backdrop_path: undefined,
        belongs_to_collection: undefined,
        budget: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        genres: f.helpers.multiple(() => undefined),
        homepage: f.internet.url(),
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        imdb_id: undefined,
        original_language: f.string.sample(),
        original_title: f.string.sample(),
        overview: f.string.sample(),
        popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        poster_path: undefined,
        production_companies: f.helpers.multiple(() => undefined),
        production_countries: f.helpers.multiple(() => undefined),
        release_date: f.string.sample(),
        revenue: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        runtime: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        spoken_languages: f.helpers.multiple(() => undefined),
        status: f.string.sample(),
        tagline: f.string.sample(),
        title: f.lorem.words(),
        video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
        vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3MovieByMovieIdListsRequest = (options?: Options): Omit<Get3MovieByMovieIdListsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            movie_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() }
        }
    };
};

export const fakeGet3MovieByMovieIdListsResponse = (options?: Options): Get3MovieByMovieIdListsResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            description: f.lorem.sentence(),
            favorite_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            item_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            iso_639_1: f.string.sample(),
            list_type: f.string.sample(),
            name: f.string.sample(),
            poster_path: undefined
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3MovieByMovieIdRecommendationsRequest = (options?: Options): Omit<Get3MovieByMovieIdRecommendationsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            movie_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() }
        }
    };
};

export const fakeGet3MovieByMovieIdRecommendationsResponse = (): Get3MovieByMovieIdRecommendationsResponse => ({});

export const fakeGet3MovieByMovieIdReleaseDatesRequest = (options?: Options): Omit<Get3MovieByMovieIdReleaseDatesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            movie_id: f.number.int()
        }
    };
};

export const fakeGet3MovieByMovieIdReleaseDatesResponse = (options?: Options): Get3MovieByMovieIdReleaseDatesResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            iso_3166_1: f.string.sample(),
            release_dates: f.helpers.multiple(() => ({
                certification: f.string.sample(),
                descriptors: f.helpers.multiple(() => undefined),
                iso_639_1: f.string.sample(),
                note: f.string.sample(),
                release_date: f.string.sample(),
                type: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            }))
        }))
    };
};

export const fakeGet3MovieByMovieIdReviewsRequest = (options?: Options): Omit<Get3MovieByMovieIdReviewsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            movie_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() }
        }
    };
};

export const fakeGet3MovieByMovieIdReviewsResponse = (options?: Options): Get3MovieByMovieIdReviewsResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            author: f.string.sample(),
            author_details: {
                name: f.string.sample(),
                username: f.internet.username(),
                avatar_path: f.string.sample(),
                rating: undefined
            },
            content: f.string.sample(),
            created_at: f.string.sample(),
            id: f.string.uuid(),
            updated_at: f.string.sample(),
            url: f.internet.url()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3MovieByMovieIdSimilarRequest = (options?: Options): Omit<Get3MovieByMovieIdSimilarData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            movie_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() }
        }
    };
};

export const fakeGet3MovieByMovieIdSimilarResponse = (options?: Options): Get3MovieByMovieIdSimilarResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            original_language: f.string.sample(),
            original_title: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            release_date: f.string.sample(),
            title: f.lorem.words(),
            video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3MovieByMovieIdTranslationsRequest = (options?: Options): Omit<Get3MovieByMovieIdTranslationsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            movie_id: f.number.int()
        }
    };
};

export const fakeGet3MovieByMovieIdTranslationsResponse = (options?: Options): Get3MovieByMovieIdTranslationsResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        translations: f.helpers.multiple(() => ({
            iso_3166_1: f.string.sample(),
            iso_639_1: f.string.sample(),
            name: f.string.sample(),
            english_name: f.string.sample(),
            data: {
                homepage: f.internet.url(),
                overview: f.string.sample(),
                runtime: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                tagline: f.string.sample(),
                title: f.lorem.words()
            }
        }))
    };
};

export const fakeGet3MovieByMovieIdVideosRequest = (options?: Options): Omit<Get3MovieByMovieIdVideosData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            movie_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3MovieByMovieIdVideosResponse = (options?: Options): Get3MovieByMovieIdVideosResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            iso_639_1: f.string.sample(),
            iso_3166_1: f.string.sample(),
            name: f.string.sample(),
            key: f.string.sample(),
            site: f.string.sample(),
            size: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            type: f.string.sample(),
            official: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            published_at: f.string.sample(),
            id: f.string.uuid()
        }))
    };
};

export const fakeGet3MovieByMovieIdWatchProvidersRequest = (options?: Options): Omit<Get3MovieByMovieIdWatchProvidersData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            movie_id: f.number.int()
        }
    };
};

export const fakeGet3MovieByMovieIdWatchProvidersResponse = (options?: Options): Get3MovieByMovieIdWatchProvidersResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: {
            AE: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            AL: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            AR: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            AT: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            AU: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BA: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BB: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BE: {
                link: f.string.sample(),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BG: {
                link: f.string.sample(),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BH: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BO: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BR: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BS: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            CA: {
                link: f.string.sample(),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            CH: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            CL: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            CO: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            CR: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            CV: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            CZ: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            DE: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            DK: {
                link: f.string.sample(),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            DO: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            EC: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            EE: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            EG: {
                link: f.string.sample(),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            ES: {
                link: f.string.sample(),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                ads: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            FI: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            FJ: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            FR: {
                link: f.string.sample(),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            GB: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            GF: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            GI: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            GR: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            GT: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            HK: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            HN: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            HR: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                ads: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            HU: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            ID: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            IE: {
                link: f.string.sample(),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            IL: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            IN: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            IQ: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            IS: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            IT: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            JM: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            JO: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            JP: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            KR: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            KW: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            LB: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            LI: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            LT: {
                link: f.string.sample(),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            LV: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            MD: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            MK: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            MT: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            MU: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            MX: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            MY: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            MZ: {
                link: f.string.sample(),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            NL: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            NO: {
                link: f.string.sample(),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            NZ: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            OM: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            PA: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            PE: {
                link: f.string.sample(),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            PH: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            PK: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            PL: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            PS: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            PT: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            PY: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            QA: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            RO: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            RS: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            RU: {
                link: f.string.sample(),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            SA: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            SE: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            SG: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            SI: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            SK: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            SM: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            SV: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            TH: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            TR: {
                link: f.string.sample(),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            TT: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            TW: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            UG: {
                link: f.string.sample(),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            US: {
                link: f.string.sample(),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            UY: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            VE: {
                link: f.string.sample(),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            YE: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            ZA: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            }
        }
    };
};

export const fakeDelete3MovieByMovieIdRatingRequest = (options?: Options): Omit<Delete3MovieByMovieIdRatingData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        headers: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'Content-Type': resolveCondition(options?.useDefault ?? false, f) ? 'application/json;charset=utf-8' : f.system.mimeType() }
        },
        path: {
            movie_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { guest_session_id: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { session_id: f.string.sample() }
        }
    };
};

export const fakeDelete3MovieByMovieIdRatingResponse = (options?: Options): Delete3MovieByMovieIdRatingResponse => {
    const f = options?.faker ?? faker;
    return {
        status_code: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        status_message: f.string.sample()
    };
};

export const fakePost3MovieByMovieIdRatingRequest = (options?: Options): Omit<Post3MovieByMovieIdRatingData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        body: {
            RAW_BODY: f.string.sample()
        },
        headers: {
            'Content-Type': resolveCondition(options?.useDefault ?? false, f) ? 'application/json;charset=utf-8' : f.system.mimeType()
        },
        path: {
            movie_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { guest_session_id: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { session_id: f.string.sample() }
        }
    };
};

export const fakePost3MovieByMovieIdRatingResponse = (options?: Options): Post3MovieByMovieIdRatingResponse => {
    const f = options?.faker ?? faker;
    return {
        status_code: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        status_message: f.string.sample()
    };
};

export const fakeGet3NetworkByNetworkIdRequest = (options?: Options): Omit<Get3NetworkByNetworkIdData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            network_id: f.number.int()
        }
    };
};

export const fakeGet3NetworkByNetworkIdResponse = (options?: Options): Get3NetworkByNetworkIdResponse => {
    const f = options?.faker ?? faker;
    return {
        headquarters: f.string.sample(),
        homepage: f.internet.url(),
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        logo_path: f.string.sample(),
        name: f.string.sample(),
        origin_country: f.string.sample()
    };
};

export const fakeGet3NetworkByNetworkIdAlternativeNamesRequest = (options?: Options): Omit<Get3NetworkByNetworkIdAlternativeNamesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            network_id: f.number.int()
        }
    };
};

export const fakeGet3NetworkByNetworkIdAlternativeNamesResponse = (options?: Options): Get3NetworkByNetworkIdAlternativeNamesResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            name: f.string.sample(),
            type: f.string.sample()
        }))
    };
};

export const fakeGet3NetworkByNetworkIdImagesRequest = (options?: Options): Omit<Get3NetworkByNetworkIdImagesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            network_id: f.number.int()
        }
    };
};

export const fakeGet3NetworkByNetworkIdImagesResponse = (options?: Options): Get3NetworkByNetworkIdImagesResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        logos: f.helpers.multiple(() => ({
            aspect_ratio: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            file_path: f.system.filePath(),
            height: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: f.string.uuid(),
            file_type: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            width: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        }))
    };
};

export const fakeGet3PersonPopularRequest = (options?: Options): Omit<Get3PersonPopularData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() }
        }
    };
};

export const fakeGet3PersonPopularResponse = (options?: Options): Get3PersonPopularResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            known_for: f.helpers.multiple(() => ({
                adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
                backdrop_path: f.string.sample(),
                genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
                id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                media_type: f.string.sample(),
                original_language: f.string.sample(),
                original_title: f.string.sample(),
                overview: f.string.sample(),
                poster_path: f.string.sample(),
                release_date: f.string.sample(),
                title: f.lorem.words(),
                video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
                vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
                vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            known_for_department: f.string.sample(),
            name: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            profile_path: f.string.sample()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3PersonByPersonIdRequest = (options?: Options): Omit<Get3PersonByPersonIdData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            person_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { append_to_response: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3PersonByPersonIdResponse = (options?: Options): Get3PersonByPersonIdResponse => {
    const f = options?.faker ?? faker;
    return {
        adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
        also_known_as: f.helpers.multiple(() => f.string.sample()),
        biography: f.string.sample(),
        birthday: f.string.sample(),
        deathday: undefined,
        gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        homepage: undefined,
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        imdb_id: f.string.sample(),
        known_for_department: f.string.sample(),
        name: f.string.sample(),
        place_of_birth: f.string.sample(),
        popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
        profile_path: f.string.sample()
    };
};

export const fakeGet3PersonByPersonIdChangesRequest = (options?: Options): Omit<Get3PersonByPersonIdChangesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            person_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { end_date: f.date.recent().toISOString().slice(0, 10) },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { start_date: f.date.recent().toISOString().slice(0, 10) }
        }
    };
};

export const fakeGet3PersonByPersonIdChangesResponse = (options?: Options): Get3PersonByPersonIdChangesResponse => {
    const f = options?.faker ?? faker;
    return {
        changes: f.helpers.multiple(() => ({
            key: f.string.sample(),
            items: f.helpers.multiple(() => ({
                id: f.string.uuid(),
                action: f.string.sample(),
                time: f.string.sample(),
                iso_639_1: f.string.sample(),
                iso_3166_1: f.string.sample(),
                value: f.string.sample()
            }))
        }))
    };
};

export const fakeGet3PersonByPersonIdCombinedCreditsRequest = (options?: Options): Omit<Get3PersonByPersonIdCombinedCreditsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            person_id: f.string.sample()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3PersonByPersonIdCombinedCreditsResponse = (options?: Options): Get3PersonByPersonIdCombinedCreditsResponse => {
    const f = options?.faker ?? faker;
    return {
        cast: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            original_language: f.string.sample(),
            original_title: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            release_date: f.string.sample(),
            title: f.lorem.words(),
            video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            character: f.string.sample(),
            credit_id: f.string.sample(),
            order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            media_type: f.string.sample()
        })),
        crew: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            original_language: f.string.sample(),
            original_title: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            release_date: f.string.sample(),
            title: f.lorem.words(),
            video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            credit_id: f.string.sample(),
            department: f.string.sample(),
            job: f.string.sample(),
            media_type: f.string.sample()
        })),
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3PersonByPersonIdExternalIdsRequest = (options?: Options): Omit<Get3PersonByPersonIdExternalIdsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            person_id: f.number.int()
        }
    };
};

export const fakeGet3PersonByPersonIdExternalIdsResponse = (options?: Options): Get3PersonByPersonIdExternalIdsResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        freebase_mid: f.string.sample(),
        freebase_id: f.string.sample(),
        imdb_id: f.string.sample(),
        tvrage_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        wikidata_id: f.string.sample(),
        facebook_id: f.string.sample(),
        instagram_id: f.string.sample(),
        tiktok_id: f.string.sample(),
        twitter_id: f.string.sample(),
        youtube_id: undefined
    };
};

export const fakeGet3PersonByPersonIdImagesRequest = (options?: Options): Omit<Get3PersonByPersonIdImagesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            person_id: f.number.int()
        }
    };
};

export const fakeGet3PersonByPersonIdImagesResponse = (options?: Options): Get3PersonByPersonIdImagesResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        profiles: f.helpers.multiple(() => ({
            aspect_ratio: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            height: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            iso_639_1: undefined,
            file_path: f.system.filePath(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            width: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        }))
    };
};

export const fakeGet3PersonLatestResponse = (options?: Options): Get3PersonLatestResponse => {
    const f = options?.faker ?? faker;
    return {
        adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
        also_known_as: f.helpers.multiple(() => undefined),
        biography: f.string.sample(),
        birthday: undefined,
        deathday: undefined,
        gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        homepage: undefined,
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        imdb_id: undefined,
        known_for_department: undefined,
        name: f.string.sample(),
        place_of_birth: undefined,
        popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        profile_path: undefined
    };
};

export const fakeGet3PersonByPersonIdMovieCreditsRequest = (options?: Options): Omit<Get3PersonByPersonIdMovieCreditsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            person_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3PersonByPersonIdMovieCreditsResponse = (options?: Options): Get3PersonByPersonIdMovieCreditsResponse => {
    const f = options?.faker ?? faker;
    return {
        cast: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            original_language: f.string.sample(),
            original_title: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            release_date: f.string.sample(),
            title: f.lorem.words(),
            video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            character: f.string.sample(),
            credit_id: f.string.sample(),
            order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        crew: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            original_language: f.string.sample(),
            original_title: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            release_date: f.string.sample(),
            title: f.lorem.words(),
            video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            credit_id: f.string.sample(),
            department: f.string.sample(),
            job: f.string.sample()
        })),
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3PersonByPersonIdTvCreditsRequest = (options?: Options): Omit<Get3PersonByPersonIdTvCreditsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            person_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3PersonByPersonIdTvCreditsResponse = (options?: Options): Get3PersonByPersonIdTvCreditsResponse => {
    const f = options?.faker ?? faker;
    return {
        cast: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            origin_country: f.helpers.multiple(() => f.string.sample()),
            original_language: f.string.sample(),
            original_name: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            first_air_date: f.string.sample(),
            name: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            character: f.string.sample(),
            credit_id: f.string.sample(),
            episode_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        crew: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            origin_country: f.helpers.multiple(() => f.string.sample()),
            original_language: f.string.sample(),
            original_name: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            first_air_date: f.string.sample(),
            name: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            credit_id: f.string.sample(),
            department: f.string.sample(),
            episode_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            job: f.string.sample()
        })),
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3PersonByPersonIdTaggedImagesRequest = (options?: Options): Omit<Get3PersonByPersonIdTaggedImagesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            person_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() }
        }
    };
};

export const fakeGet3PersonByPersonIdTaggedImagesResponse = (options?: Options): Get3PersonByPersonIdTaggedImagesResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            aspect_ratio: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            file_path: f.system.filePath(),
            height: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: f.string.uuid(),
            iso_639_1: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            width: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            image_type: f.string.sample(),
            media: {
                adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
                backdrop_path: f.string.sample(),
                id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                title: f.lorem.words(),
                original_language: f.string.sample(),
                original_title: f.string.sample(),
                overview: f.string.sample(),
                poster_path: f.string.sample(),
                media_type: f.string.sample(),
                genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
                popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
                release_date: f.string.sample(),
                video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
                vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
                vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            },
            media_type: f.string.sample()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3PersonByPersonIdTranslationsRequest = (options?: Options): Omit<Get3PersonByPersonIdTranslationsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            person_id: f.number.int()
        }
    };
};

export const fakeGet3PersonByPersonIdTranslationsResponse = (options?: Options): Get3PersonByPersonIdTranslationsResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        translations: f.helpers.multiple(() => ({
            iso_3166_1: f.string.sample(),
            iso_639_1: f.string.sample(),
            name: f.string.sample(),
            english_name: f.string.sample(),
            data: {
                biography: f.string.sample(),
                name: f.string.sample()
            }
        }))
    };
};

export const fakeGet3ReviewByReviewIdRequest = (options?: Options): Omit<Get3ReviewByReviewIdData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            review_id: f.string.sample()
        }
    };
};

export const fakeGet3ReviewByReviewIdResponse = (options?: Options): Get3ReviewByReviewIdResponse => {
    const f = options?.faker ?? faker;
    return {
        id: f.string.uuid(),
        author: f.string.sample(),
        author_details: {
            name: f.string.sample(),
            username: f.internet.username(),
            avatar_path: f.string.sample(),
            rating: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        },
        content: f.string.sample(),
        created_at: f.string.sample(),
        iso_639_1: f.string.sample(),
        media_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        media_title: f.string.sample(),
        media_type: f.string.sample(),
        updated_at: f.string.sample(),
        url: f.internet.url()
    };
};

export const fakeGet3SearchCollectionRequest = (options?: Options): Omit<Get3SearchCollectionData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            query: f.string.sample(),
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { include_adult: resolveCondition(options?.useDefault ?? false, f) ? false : f.datatype.boolean() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { region: f.string.sample() }
        }
    };
};

export const fakeGet3SearchCollectionResponse = (options?: Options): Get3SearchCollectionResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample(),
            original_language: f.string.sample(),
            original_name: f.string.sample(),
            overview: f.string.sample(),
            poster_path: f.string.sample()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3SearchCompanyRequest = (options?: Options): Omit<Get3SearchCompanyData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            query: f.string.sample(),
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() }
        }
    };
};

export const fakeGet3SearchCompanyResponse = (options?: Options): Get3SearchCompanyResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            logo_path: f.string.sample(),
            name: f.string.sample(),
            origin_country: f.string.sample()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3SearchKeywordRequest = (options?: Options): Omit<Get3SearchKeywordData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            query: f.string.sample(),
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() }
        }
    };
};

export const fakeGet3SearchKeywordResponse = (options?: Options): Get3SearchKeywordResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3SearchMovieRequest = (options?: Options): Omit<Get3SearchMovieData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            query: f.string.sample(),
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { include_adult: resolveCondition(options?.useDefault ?? false, f) ? false : f.datatype.boolean() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { primary_release_year: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { region: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { year: f.string.sample() }
        }
    };
};

export const fakeGet3SearchMovieResponse = (options?: Options): Get3SearchMovieResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            original_language: f.string.sample(),
            original_title: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            release_date: f.string.sample(),
            title: f.lorem.words(),
            video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3SearchMultiRequest = (options?: Options): Omit<Get3SearchMultiData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            query: f.string.sample(),
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { include_adult: resolveCondition(options?.useDefault ?? false, f) ? false : f.datatype.boolean() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() }
        }
    };
};

export const fakeGet3SearchMultiResponse = (options?: Options): Get3SearchMultiResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            title: f.lorem.words(),
            original_language: f.string.sample(),
            original_title: f.string.sample(),
            overview: f.string.sample(),
            poster_path: f.string.sample(),
            media_type: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            release_date: f.string.sample(),
            video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample(),
            original_name: f.string.sample()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3SearchPersonRequest = (options?: Options): Omit<Get3SearchPersonData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            query: f.string.sample(),
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { include_adult: resolveCondition(options?.useDefault ?? false, f) ? false : f.datatype.boolean() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() }
        }
    };
};

export const fakeGet3SearchPersonResponse = (options?: Options): Get3SearchPersonResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            known_for_department: f.string.sample(),
            name: f.string.sample(),
            original_name: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            profile_path: f.string.sample(),
            known_for: f.helpers.multiple(() => ({
                adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
                backdrop_path: f.string.sample(),
                id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                title: f.lorem.words(),
                original_language: f.string.sample(),
                original_title: f.string.sample(),
                overview: f.string.sample(),
                poster_path: f.string.sample(),
                media_type: f.string.sample(),
                genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
                popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
                release_date: f.string.sample(),
                video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
                vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
                vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            }))
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3SearchTvRequest = (options?: Options): Omit<Get3SearchTvData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            query: f.string.sample(),
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { first_air_date_year: f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { include_adult: resolveCondition(options?.useDefault ?? false, f) ? false : f.datatype.boolean() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { year: f.number.int() }
        }
    };
};

export const fakeGet3SearchTvResponse = (options?: Options): Get3SearchTvResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            origin_country: f.helpers.multiple(() => f.string.sample()),
            original_language: f.string.sample(),
            original_name: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            first_air_date: f.string.sample(),
            name: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TrendingAllByTimeWindowRequest = (options?: Options): Omit<Get3TrendingAllByTimeWindowData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            time_window: resolveCondition(options?.useDefault ?? false, f) ? 'day' : f.helpers.arrayElement(['day', 'week'])
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3TrendingAllByTimeWindowResponse = (options?: Options): Get3TrendingAllByTimeWindowResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            title: f.lorem.words(),
            original_language: f.string.sample(),
            original_title: f.string.sample(),
            overview: f.string.sample(),
            poster_path: f.string.sample(),
            media_type: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            release_date: f.string.sample(),
            video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TrendingMovieByTimeWindowRequest = (options?: Options): Omit<Get3TrendingMovieByTimeWindowData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            time_window: resolveCondition(options?.useDefault ?? false, f) ? 'day' : f.helpers.arrayElement(['day', 'week'])
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3TrendingMovieByTimeWindowResponse = (options?: Options): Get3TrendingMovieByTimeWindowResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            title: f.lorem.words(),
            original_language: f.string.sample(),
            original_title: f.string.sample(),
            overview: f.string.sample(),
            poster_path: f.string.sample(),
            media_type: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            release_date: f.string.sample(),
            video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TrendingPersonByTimeWindowRequest = (options?: Options): Omit<Get3TrendingPersonByTimeWindowData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            time_window: resolveCondition(options?.useDefault ?? false, f) ? 'day' : f.helpers.arrayElement(['day', 'week'])
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3TrendingPersonByTimeWindowResponse = (options?: Options): Get3TrendingPersonByTimeWindowResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample(),
            original_name: f.string.sample(),
            media_type: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            known_for_department: f.string.sample(),
            profile_path: f.string.sample(),
            known_for: f.helpers.multiple(() => ({
                adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
                backdrop_path: f.string.sample(),
                id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                title: f.lorem.words(),
                original_language: f.string.sample(),
                original_title: f.string.sample(),
                overview: f.string.sample(),
                poster_path: f.string.sample(),
                media_type: f.string.sample(),
                genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
                popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
                release_date: f.string.sample(),
                video: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
                vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
                vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            }))
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TrendingTvByTimeWindowRequest = (options?: Options): Omit<Get3TrendingTvByTimeWindowData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            time_window: resolveCondition(options?.useDefault ?? false, f) ? 'day' : f.helpers.arrayElement(['day', 'week'])
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3TrendingTvByTimeWindowResponse = (options?: Options): Get3TrendingTvByTimeWindowResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample(),
            original_language: f.string.sample(),
            original_name: f.string.sample(),
            overview: f.string.sample(),
            poster_path: f.string.sample(),
            media_type: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            first_air_date: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            origin_country: f.helpers.multiple(() => f.string.sample())
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TvAiringTodayRequest = (options?: Options): Omit<Get3TvAiringTodayData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { timezone: f.location.timeZone() }
        }
    };
};

export const fakeGet3TvAiringTodayResponse = (options?: Options): Get3TvAiringTodayResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            backdrop_path: f.string.sample(),
            first_air_date: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample(),
            origin_country: f.helpers.multiple(() => f.string.sample()),
            original_language: f.string.sample(),
            original_name: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TvOnTheAirRequest = (options?: Options): Omit<Get3TvOnTheAirData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { timezone: f.location.timeZone() }
        }
    };
};

export const fakeGet3TvOnTheAirResponse = (options?: Options): Get3TvOnTheAirResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            backdrop_path: f.string.sample(),
            first_air_date: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample(),
            origin_country: f.helpers.multiple(() => f.string.sample()),
            original_language: f.string.sample(),
            original_name: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TvPopularRequest = (options?: Options): Omit<Get3TvPopularData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() }
        }
    };
};

export const fakeGet3TvPopularResponse = (options?: Options): Get3TvPopularResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            backdrop_path: f.string.sample(),
            first_air_date: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample(),
            origin_country: f.helpers.multiple(() => f.string.sample()),
            original_language: f.string.sample(),
            original_name: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TvTopRatedRequest = (options?: Options): Omit<Get3TvTopRatedData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() }
        }
    };
};

export const fakeGet3TvTopRatedResponse = (options?: Options): Get3TvTopRatedResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            backdrop_path: f.string.sample(),
            first_air_date: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample(),
            origin_country: f.helpers.multiple(() => f.string.sample()),
            original_language: f.string.sample(),
            original_name: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TvBySeriesIdRequest = (options?: Options): Omit<Get3TvBySeriesIdData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { append_to_response: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3TvBySeriesIdResponse = (options?: Options): Get3TvBySeriesIdResponse => {
    const f = options?.faker ?? faker;
    return {
        adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
        backdrop_path: f.string.sample(),
        created_by: f.helpers.multiple(() => ({
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            credit_id: f.string.sample(),
            name: f.string.sample(),
            gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            profile_path: f.string.sample()
        })),
        episode_run_time: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
        first_air_date: f.string.sample(),
        genres: f.helpers.multiple(() => ({
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample()
        })),
        homepage: f.internet.url(),
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        in_production: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
        languages: f.helpers.multiple(() => f.string.sample()),
        last_air_date: f.string.sample(),
        last_episode_to_air: {
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample(),
            overview: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            air_date: f.string.sample(),
            episode_number: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            production_code: f.string.sample(),
            runtime: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            season_number: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            show_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            still_path: f.string.sample()
        },
        name: f.string.sample(),
        next_episode_to_air: undefined,
        networks: f.helpers.multiple(() => ({
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            logo_path: f.string.sample(),
            name: f.string.sample(),
            origin_country: f.string.sample()
        })),
        number_of_episodes: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        number_of_seasons: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        origin_country: f.helpers.multiple(() => f.string.sample()),
        original_language: f.string.sample(),
        original_name: f.string.sample(),
        overview: f.string.sample(),
        popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
        poster_path: f.string.sample(),
        production_companies: f.helpers.multiple(() => ({
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            logo_path: f.string.sample(),
            name: f.string.sample(),
            origin_country: f.string.sample()
        })),
        production_countries: f.helpers.multiple(() => ({
            iso_3166_1: f.string.sample(),
            name: f.string.sample()
        })),
        seasons: f.helpers.multiple(() => ({
            air_date: f.string.sample(),
            episode_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample(),
            overview: f.string.sample(),
            poster_path: f.string.sample(),
            season_number: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        spoken_languages: f.helpers.multiple(() => ({
            english_name: f.string.sample(),
            iso_639_1: f.string.sample(),
            name: f.string.sample()
        })),
        status: f.string.sample(),
        tagline: f.string.sample(),
        type: f.string.sample(),
        vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
        vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TvBySeriesIdAccountStatesRequest = (options?: Options): Omit<Get3TvBySeriesIdAccountStatesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { session_id: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { guest_session_id: f.string.sample() }
        }
    };
};

export const fakeGet3TvBySeriesIdAccountStatesResponse = (options?: Options): Get3TvBySeriesIdAccountStatesResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        favorite: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
        rated: {
            value: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        },
        watchlist: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean()
    };
};

export const fakeGet3TvBySeriesIdAggregateCreditsRequest = (options?: Options): Omit<Get3TvBySeriesIdAggregateCreditsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3TvBySeriesIdAggregateCreditsResponse = (options?: Options): Get3TvBySeriesIdAggregateCreditsResponse => {
    const f = options?.faker ?? faker;
    return {
        cast: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            known_for_department: f.string.sample(),
            name: f.string.sample(),
            original_name: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            profile_path: f.string.sample(),
            roles: f.helpers.multiple(() => ({
                credit_id: f.string.sample(),
                character: f.string.sample(),
                episode_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            total_episode_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        crew: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            known_for_department: f.string.sample(),
            name: f.string.sample(),
            original_name: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            profile_path: f.string.sample(),
            jobs: f.helpers.multiple(() => ({
                credit_id: f.string.sample(),
                job: f.string.sample(),
                episode_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            department: f.string.sample(),
            total_episode_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TvBySeriesIdAlternativeTitlesRequest = (options?: Options): Omit<Get3TvBySeriesIdAlternativeTitlesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int()
        }
    };
};

export const fakeGet3TvBySeriesIdAlternativeTitlesResponse = (options?: Options): Get3TvBySeriesIdAlternativeTitlesResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            iso_3166_1: f.string.sample(),
            title: f.lorem.words(),
            type: f.string.sample()
        }))
    };
};

export const fakeGet3TvBySeriesIdChangesRequest = (options?: Options): Omit<Get3TvBySeriesIdChangesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { end_date: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { start_date: f.string.sample() }
        }
    };
};

export const fakeGet3TvBySeriesIdChangesResponse = (options?: Options): Get3TvBySeriesIdChangesResponse => {
    const f = options?.faker ?? faker;
    return {
        changes: f.helpers.multiple(() => ({
            key: f.string.sample(),
            items: f.helpers.multiple(() => ({
                id: f.string.uuid(),
                action: f.string.sample(),
                time: f.string.sample(),
                iso_639_1: f.string.sample(),
                iso_3166_1: f.string.sample(),
                value: {
                    poster: {
                        file_path: f.system.filePath(),
                        iso_639_1: f.string.sample()
                    }
                },
                original_value: {
                    poster: {
                        file_path: f.system.filePath(),
                        iso_639_1: f.string.sample()
                    }
                }
            }))
        }))
    };
};

export const fakeGet3TvBySeriesIdContentRatingsRequest = (options?: Options): Omit<Get3TvBySeriesIdContentRatingsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int()
        }
    };
};

export const fakeGet3TvBySeriesIdContentRatingsResponse = (options?: Options): Get3TvBySeriesIdContentRatingsResponse => {
    const f = options?.faker ?? faker;
    return {
        results: f.helpers.multiple(() => ({
            descriptors: f.helpers.multiple(() => undefined),
            iso_3166_1: f.string.sample(),
            rating: f.string.sample()
        })),
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TvBySeriesIdCreditsRequest = (options?: Options): Omit<Get3TvBySeriesIdCreditsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3TvBySeriesIdCreditsResponse = (options?: Options): Get3TvBySeriesIdCreditsResponse => {
    const f = options?.faker ?? faker;
    return {
        cast: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            known_for_department: f.string.sample(),
            name: f.string.sample(),
            original_name: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            profile_path: f.string.sample(),
            character: f.string.sample(),
            credit_id: f.string.sample(),
            order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        crew: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            known_for_department: f.string.sample(),
            name: f.string.sample(),
            original_name: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            profile_path: f.string.sample(),
            credit_id: f.string.sample(),
            department: f.string.sample(),
            job: f.string.sample()
        })),
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TvBySeriesIdEpisodeGroupsRequest = (options?: Options): Omit<Get3TvBySeriesIdEpisodeGroupsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int()
        }
    };
};

export const fakeGet3TvBySeriesIdEpisodeGroupsResponse = (options?: Options): Get3TvBySeriesIdEpisodeGroupsResponse => {
    const f = options?.faker ?? faker;
    return {
        results: f.helpers.multiple(() => ({
            description: f.lorem.sentence(),
            episode_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            group_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: f.string.uuid(),
            name: f.string.sample(),
            network: {
                id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                logo_path: f.string.sample(),
                name: f.string.sample(),
                origin_country: f.string.sample()
            },
            type: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TvBySeriesIdExternalIdsRequest = (options?: Options): Omit<Get3TvBySeriesIdExternalIdsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int()
        }
    };
};

export const fakeGet3TvBySeriesIdExternalIdsResponse = (options?: Options): Get3TvBySeriesIdExternalIdsResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        imdb_id: f.string.sample(),
        freebase_mid: f.string.sample(),
        freebase_id: f.string.sample(),
        tvdb_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        tvrage_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        wikidata_id: f.string.sample(),
        facebook_id: f.string.sample(),
        instagram_id: f.string.sample(),
        twitter_id: f.string.sample()
    };
};

export const fakeGet3TvBySeriesIdImagesRequest = (options?: Options): Omit<Get3TvBySeriesIdImagesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { include_image_language: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: f.string.sample() }
        }
    };
};

export const fakeGet3TvBySeriesIdImagesResponse = (options?: Options): Get3TvBySeriesIdImagesResponse => {
    const f = options?.faker ?? faker;
    return {
        backdrops: f.helpers.multiple(() => ({
            aspect_ratio: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            height: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            iso_639_1: undefined,
            file_path: f.system.filePath(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            width: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        logos: f.helpers.multiple(() => ({
            aspect_ratio: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            height: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            iso_639_1: f.string.sample(),
            file_path: f.system.filePath(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            width: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        posters: f.helpers.multiple(() => ({
            aspect_ratio: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            height: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            iso_639_1: f.string.sample(),
            file_path: f.system.filePath(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            width: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        }))
    };
};

export const fakeGet3TvBySeriesIdKeywordsRequest = (options?: Options): Omit<Get3TvBySeriesIdKeywordsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int()
        }
    };
};

export const fakeGet3TvBySeriesIdKeywordsResponse = (options?: Options): Get3TvBySeriesIdKeywordsResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            name: f.string.sample(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        }))
    };
};

export const fakeGet3TvLatestResponse = (options?: Options): Get3TvLatestResponse => {
    const f = options?.faker ?? faker;
    return {
        adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
        backdrop_path: undefined,
        created_by: f.helpers.multiple(() => undefined),
        episode_run_time: f.helpers.multiple(() => undefined),
        first_air_date: f.string.sample(),
        genres: f.helpers.multiple(() => undefined),
        homepage: f.internet.url(),
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        in_production: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
        languages: f.helpers.multiple(() => undefined),
        last_air_date: f.string.sample(),
        last_episode_to_air: {
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample(),
            overview: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            air_date: f.string.sample(),
            episode_number: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            production_code: f.string.sample(),
            runtime: undefined,
            season_number: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            show_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            still_path: undefined
        },
        name: f.string.sample(),
        next_episode_to_air: undefined,
        networks: f.helpers.multiple(() => undefined),
        number_of_episodes: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        number_of_seasons: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        origin_country: f.helpers.multiple(() => f.string.sample()),
        original_language: f.string.sample(),
        original_name: f.string.sample(),
        overview: f.string.sample(),
        popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        poster_path: undefined,
        production_companies: f.helpers.multiple(() => undefined),
        production_countries: f.helpers.multiple(() => undefined),
        seasons: f.helpers.multiple(() => ({
            air_date: undefined,
            episode_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample(),
            overview: f.string.sample(),
            poster_path: undefined,
            season_number: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        spoken_languages: f.helpers.multiple(() => undefined),
        status: f.string.sample(),
        tagline: f.string.sample(),
        type: f.string.sample(),
        vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TvBySeriesIdListsRequest = (options?: Options): Omit<Get3TvBySeriesIdListsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() }
        }
    };
};

export const fakeGet3TvBySeriesIdListsResponse = (options?: Options): Get3TvBySeriesIdListsResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            description: f.lorem.sentence(),
            favorite_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            item_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            iso_639_1: f.string.sample(),
            iso_3166_1: f.string.sample(),
            name: f.string.sample(),
            poster_path: undefined
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TvBySeriesIdRecommendationsRequest = (options?: Options): Omit<Get3TvBySeriesIdRecommendationsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() }
        }
    };
};

export const fakeGet3TvBySeriesIdRecommendationsResponse = (options?: Options): Get3TvBySeriesIdRecommendationsResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample(),
            original_language: f.string.sample(),
            original_name: f.string.sample(),
            overview: f.string.sample(),
            poster_path: f.string.sample(),
            media_type: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            first_air_date: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            origin_country: f.helpers.multiple(() => f.string.sample())
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TvBySeriesIdReviewsRequest = (options?: Options): Omit<Get3TvBySeriesIdReviewsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() }
        }
    };
};

export const fakeGet3TvBySeriesIdReviewsResponse = (options?: Options): Get3TvBySeriesIdReviewsResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            author: f.string.sample(),
            author_details: {
                name: f.string.sample(),
                username: f.internet.username(),
                avatar_path: f.string.sample(),
                rating: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            },
            content: f.string.sample(),
            created_at: f.string.sample(),
            id: f.string.uuid(),
            updated_at: f.string.sample(),
            url: f.internet.url()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TvBySeriesIdScreenedTheatricallyRequest = (options?: Options): Omit<Get3TvBySeriesIdScreenedTheatricallyData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int()
        }
    };
};

export const fakeGet3TvBySeriesIdScreenedTheatricallyResponse = (options?: Options): Get3TvBySeriesIdScreenedTheatricallyResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            episode_number: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            season_number: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        }))
    };
};

export const fakeGet3TvBySeriesIdSimilarRequest = (options?: Options): Omit<Get3TvBySeriesIdSimilarData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.string.sample()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() }
        }
    };
};

export const fakeGet3TvBySeriesIdSimilarResponse = (options?: Options): Get3TvBySeriesIdSimilarResponse => {
    const f = options?.faker ?? faker;
    return {
        page: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            backdrop_path: f.string.sample(),
            genre_ids: f.helpers.multiple(() => resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            origin_country: f.helpers.multiple(() => f.string.sample()),
            original_language: f.string.sample(),
            original_name: f.string.sample(),
            overview: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            poster_path: f.string.sample(),
            first_air_date: f.string.sample(),
            name: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        total_pages: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        total_results: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TvBySeriesIdTranslationsRequest = (options?: Options): Omit<Get3TvBySeriesIdTranslationsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int()
        }
    };
};

export const fakeGet3TvBySeriesIdTranslationsResponse = (options?: Options): Get3TvBySeriesIdTranslationsResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        translations: f.helpers.multiple(() => ({
            iso_3166_1: f.string.sample(),
            iso_639_1: f.string.sample(),
            name: f.string.sample(),
            english_name: f.string.sample(),
            data: {
                name: f.string.sample(),
                overview: f.string.sample(),
                homepage: f.internet.url(),
                tagline: f.string.sample()
            }
        }))
    };
};

export const fakeGet3TvBySeriesIdVideosRequest = (options?: Options): Omit<Get3TvBySeriesIdVideosData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { include_video_language: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3TvBySeriesIdVideosResponse = (options?: Options): Get3TvBySeriesIdVideosResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            iso_639_1: f.string.sample(),
            iso_3166_1: f.string.sample(),
            name: f.string.sample(),
            key: f.string.sample(),
            site: f.string.sample(),
            size: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            type: f.string.sample(),
            official: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            published_at: f.string.sample(),
            id: f.string.uuid()
        }))
    };
};

export const fakeGet3TvBySeriesIdWatchProvidersRequest = (options?: Options): Omit<Get3TvBySeriesIdWatchProvidersData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int()
        }
    };
};

export const fakeGet3TvBySeriesIdWatchProvidersResponse = (options?: Options): Get3TvBySeriesIdWatchProvidersResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: {
            AE: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            AR: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            AT: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            AU: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BA: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BB: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BE: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BG: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BO: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BR: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BS: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            CA: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            CH: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            CI: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            CL: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            CO: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            CR: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            CZ: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            DE: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            DK: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            DO: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            DZ: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            EC: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            EG: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            ES: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            FI: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            FR: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            GB: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            GF: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            GH: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            GQ: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            GT: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            HK: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            HN: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            HR: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            HU: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            ID: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            IE: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            IL: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            IQ: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            IT: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            JM: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            JP: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            KE: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            KR: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            LB: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            LT: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            LY: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            MD: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            MK: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            MU: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            MX: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            MY: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            MZ: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            NE: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            NG: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            NL: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            NO: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            NZ: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            PA: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            PE: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            PH: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            PL: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            PS: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            PT: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            PY: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            RO: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            RS: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            RU: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                ads: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            SA: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            SC: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            SE: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            SG: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            SI: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            SK: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            SN: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            SV: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            TH: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            TR: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            TT: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            TW: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            TZ: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            UG: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            US: {
                link: f.string.sample(),
                free: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            UY: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            VE: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            ZA: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            ZM: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            }
        }
    };
};

export const fakeDelete3TvBySeriesIdRatingRequest = (options?: Options): Omit<Delete3TvBySeriesIdRatingData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        headers: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'Content-Type': resolveCondition(options?.useDefault ?? false, f) ? 'application/json;charset=utf-8' : f.system.mimeType() }
        },
        path: {
            series_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { guest_session_id: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { session_id: f.string.sample() }
        }
    };
};

export const fakeDelete3TvBySeriesIdRatingResponse = (options?: Options): Delete3TvBySeriesIdRatingResponse => {
    const f = options?.faker ?? faker;
    return {
        status_code: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        status_message: f.string.sample()
    };
};

export const fakePost3TvBySeriesIdRatingRequest = (options?: Options): Omit<Post3TvBySeriesIdRatingData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        body: {
            RAW_BODY: f.string.sample()
        },
        headers: {
            'Content-Type': resolveCondition(options?.useDefault ?? false, f) ? 'application/json;charset=utf-8' : f.system.mimeType()
        },
        path: {
            series_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { guest_session_id: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { session_id: f.string.sample() }
        }
    };
};

export const fakePost3TvBySeriesIdRatingResponse = (options?: Options): Post3TvBySeriesIdRatingResponse => {
    const f = options?.faker ?? faker;
    return {
        status_code: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        status_message: f.string.sample()
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberRequest = (options?: Options): Omit<Get3TvBySeriesIdSeasonBySeasonNumberData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int(),
            season_number: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { append_to_response: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberResponse = (options?: Options): Get3TvBySeriesIdSeasonBySeasonNumberResponse => {
    const f = options?.faker ?? faker;
    return {
        _id: f.string.uuid(),
        air_date: f.string.sample(),
        episodes: f.helpers.multiple(() => ({
            air_date: f.string.sample(),
            episode_number: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            episode_type: f.string.sample(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            name: f.string.sample(),
            overview: f.string.sample(),
            production_code: f.string.sample(),
            runtime: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            season_number: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            show_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            still_path: f.string.sample(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            crew: f.helpers.multiple(() => ({
                department: f.string.sample(),
                job: f.string.sample(),
                credit_id: f.string.sample(),
                adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
                gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                known_for_department: f.string.sample(),
                name: f.string.sample(),
                original_name: f.string.sample(),
                popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
                profile_path: f.string.sample()
            })),
            guest_stars: f.helpers.multiple(() => ({
                character: f.string.sample(),
                credit_id: f.string.sample(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
                gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                known_for_department: f.string.sample(),
                name: f.string.sample(),
                original_name: f.string.sample(),
                popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
                profile_path: f.string.sample()
            }))
        })),
        name: f.string.sample(),
        networks: f.helpers.multiple(() => ({
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            logo_path: f.string.sample(),
            name: f.string.sample(),
            origin_country: f.string.sample()
        })),
        overview: f.string.sample(),
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        poster_path: f.string.sample(),
        season_number: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float()
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberAccountStatesRequest = (options?: Options): Omit<Get3TvBySeriesIdSeasonBySeasonNumberAccountStatesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int(),
            season_number: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { session_id: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { guest_session_id: f.string.sample() }
        }
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberAccountStatesResponse = (options?: Options): Get3TvBySeriesIdSeasonBySeasonNumberAccountStatesResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            episode_number: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            rated: {
                value: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            }
        }))
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberAggregateCreditsRequest = (options?: Options): Omit<Get3TvBySeriesIdSeasonBySeasonNumberAggregateCreditsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int(),
            season_number: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberAggregateCreditsResponse = (options?: Options): Get3TvBySeriesIdSeasonBySeasonNumberAggregateCreditsResponse => {
    const f = options?.faker ?? faker;
    return {
        cast: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            known_for_department: f.string.sample(),
            name: f.string.sample(),
            original_name: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            profile_path: f.string.sample(),
            roles: f.helpers.multiple(() => ({
                credit_id: f.string.sample(),
                character: f.string.sample(),
                episode_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            total_episode_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        crew: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            known_for_department: f.string.sample(),
            name: f.string.sample(),
            original_name: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            profile_path: undefined,
            jobs: f.helpers.multiple(() => ({
                credit_id: f.string.sample(),
                job: f.string.sample(),
                episode_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            department: f.string.sample(),
            total_episode_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TvSeasonBySeasonIdChangesRequest = (options?: Options): Omit<Get3TvSeasonBySeasonIdChangesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            season_id: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { end_date: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { page: resolveCondition(options?.useDefault ?? false, f) ? 1 : f.number.int() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { start_date: f.string.sample() }
        }
    };
};

export const fakeGet3TvSeasonBySeasonIdChangesResponse = (options?: Options): Get3TvSeasonBySeasonIdChangesResponse => {
    const f = options?.faker ?? faker;
    return {
        changes: f.helpers.multiple(() => ({
            key: f.string.sample(),
            items: f.helpers.multiple(() => ({
                id: f.string.uuid(),
                action: f.string.sample(),
                time: f.string.sample(),
                value: {
                    episode_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    episode_number: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }
            }))
        }))
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberCreditsRequest = (options?: Options): Omit<Get3TvBySeriesIdSeasonBySeasonNumberCreditsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int(),
            season_number: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberCreditsResponse = (options?: Options): Get3TvBySeriesIdSeasonBySeasonNumberCreditsResponse => {
    const f = options?.faker ?? faker;
    return {
        cast: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            known_for_department: f.string.sample(),
            name: f.string.sample(),
            original_name: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            profile_path: f.string.sample(),
            character: f.string.sample(),
            credit_id: f.string.sample(),
            order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        crew: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            known_for_department: f.string.sample(),
            name: f.string.sample(),
            original_name: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            profile_path: undefined,
            credit_id: f.string.sample(),
            department: f.string.sample(),
            job: f.string.sample()
        })),
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberExternalIdsRequest = (options?: Options): Omit<Get3TvBySeriesIdSeasonBySeasonNumberExternalIdsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int(),
            season_number: f.number.int()
        }
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberExternalIdsResponse = (options?: Options): Get3TvBySeriesIdSeasonBySeasonNumberExternalIdsResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        freebase_mid: f.string.sample(),
        freebase_id: f.string.sample(),
        tvdb_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        tvrage_id: undefined,
        wikidata_id: f.string.sample()
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberImagesRequest = (options?: Options): Omit<Get3TvBySeriesIdSeasonBySeasonNumberImagesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int(),
            season_number: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { include_image_language: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: f.string.sample() }
        }
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberImagesResponse = (options?: Options): Get3TvBySeriesIdSeasonBySeasonNumberImagesResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        posters: f.helpers.multiple(() => ({
            aspect_ratio: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            height: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            iso_639_1: f.string.sample(),
            file_path: f.system.filePath(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            width: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        }))
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberTranslationsRequest = (options?: Options): Omit<Get3TvBySeriesIdSeasonBySeasonNumberTranslationsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int(),
            season_number: f.number.int()
        }
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberTranslationsResponse = (options?: Options): Get3TvBySeriesIdSeasonBySeasonNumberTranslationsResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        translations: f.helpers.multiple(() => ({
            iso_3166_1: f.string.sample(),
            iso_639_1: f.string.sample(),
            name: f.string.sample(),
            english_name: f.string.sample(),
            data: {
                name: f.string.sample(),
                overview: f.string.sample()
            }
        }))
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberVideosRequest = (options?: Options): Omit<Get3TvBySeriesIdSeasonBySeasonNumberVideosData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int(),
            season_number: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { include_video_language: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberVideosResponse = (options?: Options): Get3TvBySeriesIdSeasonBySeasonNumberVideosResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            iso_639_1: f.string.sample(),
            iso_3166_1: f.string.sample(),
            name: f.string.sample(),
            key: f.string.sample(),
            site: f.string.sample(),
            size: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            type: f.string.sample(),
            official: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            published_at: f.string.sample(),
            id: f.string.uuid()
        }))
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberWatchProvidersRequest = (options?: Options): Omit<Get3TvBySeriesIdSeasonBySeasonNumberWatchProvidersData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int(),
            season_number: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberWatchProvidersResponse = (options?: Options): Get3TvBySeriesIdSeasonBySeasonNumberWatchProvidersResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: {
            AD: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            AE: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            AG: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            AR: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            AT: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            AU: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BA: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BB: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BE: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BG: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BH: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BO: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BR: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BS: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            BZ: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            CA: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            CH: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            CI: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            CL: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            CM: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            CO: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            CR: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            CZ: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            DE: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            DK: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            DO: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            EC: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            EG: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            ES: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            FI: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            FR: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            GB: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            GG: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            GQ: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            GT: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            GY: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            HK: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            HN: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            HR: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            HU: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            ID: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            IE: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            IN: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            IQ: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            IT: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            JM: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            JO: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            JP: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                rent: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            KE: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            LB: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            LC: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            MC: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            MD: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            ME: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            MG: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            MK: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            ML: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            MU: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            MX: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            MY: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            MZ: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            NE: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            NG: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            NI: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            NL: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            NO: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            NZ: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            OM: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            PA: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            PE: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            PH: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            PL: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            PT: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            PY: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            QA: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            RO: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            RS: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            RU: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            SA: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            SC: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            SE: {
                link: f.string.sample(),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            SG: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            SI: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            SK: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            SN: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            SV: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            TC: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            TD: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            TH: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            TR: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            TT: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            TW: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            US: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                })),
                buy: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            UY: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            VE: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            ZA: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            ZM: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            },
            ZW: {
                link: f.string.sample(),
                flatrate: f.helpers.multiple(() => ({
                    logo_path: f.string.sample(),
                    provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                    provider_name: f.string.sample(),
                    display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
                }))
            }
        }
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberRequest = (options?: Options): Omit<Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int(),
            season_number: f.number.int(),
            episode_number: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { append_to_response: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberResponse = (options?: Options): Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberResponse => {
    const f = options?.faker ?? faker;
    return {
        air_date: f.string.sample(),
        crew: f.helpers.multiple(() => ({
            department: f.string.sample(),
            job: f.string.sample(),
            credit_id: f.string.sample(),
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            known_for_department: f.string.sample(),
            name: f.string.sample(),
            original_name: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            profile_path: f.string.sample()
        })),
        episode_number: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        guest_stars: f.helpers.multiple(() => ({
            character: f.string.sample(),
            credit_id: f.string.sample(),
            order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            known_for_department: f.string.sample(),
            name: f.string.sample(),
            original_name: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            profile_path: f.string.sample()
        })),
        name: f.string.sample(),
        overview: f.string.sample(),
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        production_code: f.string.sample(),
        runtime: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        season_number: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        still_path: f.string.sample(),
        vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
        vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberAccountStatesRequest = (options?: Options): Omit<Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberAccountStatesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int(),
            season_number: f.number.int(),
            episode_number: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { session_id: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { guest_session_id: f.string.sample() }
        }
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberAccountStatesResponse = (options?: Options): Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberAccountStatesResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        favorite: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
        rated: {
            value: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        },
        watchlist: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean()
    };
};

export const fakeGet3TvEpisodeByEpisodeIdChangesRequest = (options?: Options): Omit<Get3TvEpisodeByEpisodeIdChangesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            episode_id: f.number.int()
        }
    };
};

export const fakeGet3TvEpisodeByEpisodeIdChangesResponse = (options?: Options): Get3TvEpisodeByEpisodeIdChangesResponse => {
    const f = options?.faker ?? faker;
    return {
        changes: f.helpers.multiple(() => ({
            key: f.string.sample(),
            items: f.helpers.multiple(() => ({
                id: f.string.uuid(),
                action: f.string.sample(),
                time: f.string.sample(),
                value: f.string.sample()
            }))
        }))
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberCreditsRequest = (options?: Options): Omit<Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberCreditsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int(),
            season_number: f.number.int(),
            episode_number: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberCreditsResponse = (options?: Options): Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberCreditsResponse => {
    const f = options?.faker ?? faker;
    return {
        cast: f.helpers.multiple(() => ({
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            known_for_department: f.string.sample(),
            name: f.string.sample(),
            original_name: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            profile_path: f.string.sample(),
            character: f.string.sample(),
            credit_id: f.string.sample(),
            order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        })),
        crew: f.helpers.multiple(() => ({
            department: f.string.sample(),
            job: f.string.sample(),
            credit_id: f.string.sample(),
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            known_for_department: f.string.sample(),
            name: f.string.sample(),
            original_name: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            profile_path: f.string.sample()
        })),
        guest_stars: f.helpers.multiple(() => ({
            character: f.string.sample(),
            credit_id: f.string.sample(),
            order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            adult: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            gender: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            known_for_department: f.string.sample(),
            name: f.string.sample(),
            original_name: f.string.sample(),
            popularity: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            profile_path: f.string.sample()
        })),
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberExternalIdsRequest = (options?: Options): Omit<Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberExternalIdsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int(),
            season_number: f.number.int(),
            episode_number: f.string.sample()
        }
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberExternalIdsResponse = (options?: Options): Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberExternalIdsResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        imdb_id: f.string.sample(),
        freebase_mid: f.string.sample(),
        freebase_id: f.string.sample(),
        tvdb_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        tvrage_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        wikidata_id: f.string.sample()
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberImagesRequest = (options?: Options): Omit<Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberImagesData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int(),
            season_number: f.number.int(),
            episode_number: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { include_image_language: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: f.string.sample() }
        }
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberImagesResponse = (options?: Options): Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberImagesResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        stills: f.helpers.multiple(() => ({
            aspect_ratio: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            height: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            iso_639_1: undefined,
            file_path: f.system.filePath(),
            vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
            vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            width: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        }))
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberTranslationsRequest = (options?: Options): Omit<Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberTranslationsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int(),
            season_number: f.number.int(),
            episode_number: f.number.int()
        }
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberTranslationsResponse = (options?: Options): Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberTranslationsResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        translations: f.helpers.multiple(() => ({
            iso_3166_1: f.string.sample(),
            iso_639_1: f.string.sample(),
            name: f.string.sample(),
            english_name: f.string.sample(),
            data: {
                name: f.string.sample(),
                overview: f.string.sample()
            }
        }))
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberVideosRequest = (options?: Options): Omit<Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberVideosData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            series_id: f.number.int(),
            season_number: f.number.int(),
            episode_number: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { include_video_language: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberVideosResponse = (options?: Options): Get3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberVideosResponse => {
    const f = options?.faker ?? faker;
    return {
        id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        results: f.helpers.multiple(() => ({
            iso_639_1: f.string.sample(),
            iso_3166_1: f.string.sample(),
            name: f.string.sample(),
            key: f.string.sample(),
            site: f.string.sample(),
            size: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            type: f.string.sample(),
            official: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean(),
            published_at: f.string.sample(),
            id: f.string.uuid()
        }))
    };
};

export const fakeDelete3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberRatingRequest = (options?: Options): Omit<Delete3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberRatingData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        headers: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { 'Content-Type': resolveCondition(options?.useDefault ?? false, f) ? 'application/json;charset=utf-8' : f.system.mimeType() }
        },
        path: {
            series_id: f.number.int(),
            season_number: f.number.int(),
            episode_number: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { guest_session_id: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { session_id: f.string.sample() }
        }
    };
};

export const fakeDelete3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberRatingResponse = (options?: Options): Delete3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberRatingResponse => {
    const f = options?.faker ?? faker;
    return {
        status_code: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        status_message: f.string.sample()
    };
};

export const fakePost3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberRatingRequest = (options?: Options): Omit<Post3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberRatingData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        body: {
            RAW_BODY: f.string.sample()
        },
        headers: {
            'Content-Type': resolveCondition(options?.useDefault ?? false, f) ? 'application/json;charset=utf-8' : f.system.mimeType()
        },
        path: {
            series_id: f.number.int(),
            season_number: f.number.int(),
            episode_number: f.number.int()
        },
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { guest_session_id: f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { session_id: f.string.sample() }
        }
    };
};

export const fakePost3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberRatingResponse = (options?: Options): Post3TvBySeriesIdSeasonBySeasonNumberEpisodeByEpisodeNumberRatingResponse => {
    const f = options?.faker ?? faker;
    return {
        status_code: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        status_message: f.string.sample()
    };
};

export const fakeGet3TvEpisodeGroupByTvEpisodeGroupIdRequest = (options?: Options): Omit<Get3TvEpisodeGroupByTvEpisodeGroupIdData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        path: {
            tv_episode_group_id: f.string.sample()
        }
    };
};

export const fakeGet3TvEpisodeGroupByTvEpisodeGroupIdResponse = (options?: Options): Get3TvEpisodeGroupByTvEpisodeGroupIdResponse => {
    const f = options?.faker ?? faker;
    return {
        description: f.lorem.sentence(),
        episode_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        group_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
        groups: f.helpers.multiple(() => ({
            id: f.string.uuid(),
            name: f.string.sample(),
            order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            episodes: f.helpers.multiple(() => ({
                air_date: f.string.sample(),
                episode_number: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                name: f.string.sample(),
                overview: f.string.sample(),
                production_code: f.string.sample(),
                runtime: undefined,
                season_number: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                show_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                still_path: f.string.sample(),
                vote_average: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.float(),
                vote_count: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                order: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            })),
            locked: resolveCondition(options?.useDefault ?? false, f) ? true : f.datatype.boolean()
        })),
        id: f.string.uuid(),
        name: f.string.sample(),
        network: {
            id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            logo_path: f.string.sample(),
            name: f.string.sample(),
            origin_country: f.string.sample()
        },
        type: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
    };
};

export const fakeGet3WatchProvidersRegionsRequest = (options?: Options): Omit<Get3WatchProvidersRegionsData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() }
        }
    };
};

export const fakeGet3WatchProvidersRegionsResponse = (options?: Options): Get3WatchProvidersRegionsResponse => {
    const f = options?.faker ?? faker;
    return {
        results: f.helpers.multiple(() => ({
            iso_3166_1: f.string.sample(),
            english_name: f.string.sample(),
            native_name: f.string.sample()
        }))
    };
};

export const fakeGet3WatchProvidersMovieRequest = (options?: Options): Omit<Get3WatchProvidersMovieData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { watch_region: f.string.sample() }
        }
    };
};

export const fakeGet3WatchProvidersMovieResponse = (options?: Options): Get3WatchProvidersMovieResponse => {
    const f = options?.faker ?? faker;
    return {
        results: f.helpers.multiple(() => ({
            display_priorities: {
                CA: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                AE: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                AR: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                AT: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                AU: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                BE: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                BO: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                BR: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                BG: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                CH: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                CL: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                CO: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                CR: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                CZ: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                DE: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                DK: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                EC: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                EE: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                EG: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                ES: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                FI: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                FR: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                GB: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                GR: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                GT: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                HK: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                HN: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                HU: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                ID: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                IE: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                IN: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                IT: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                JP: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                LT: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                LV: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                MX: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                MY: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                NL: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                NO: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                NZ: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                PE: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                PH: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                PL: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                PT: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                PY: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                RU: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                SA: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                SE: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                SG: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                SK: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                TH: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                TR: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                TW: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                US: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                VE: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                ZA: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                SI: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                CV: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                GH: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                MU: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                MZ: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                UG: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                IL: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            },
            display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            logo_path: f.string.sample(),
            provider_name: f.string.sample(),
            provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        }))
    };
};

export const fakeGet3WatchProvidersTvRequest = (options?: Options): Omit<Get3WatchProvidersTvData, 'url'> => {
    const f = options?.faker ?? faker;
    return {
        query: {
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { language: resolveCondition(options?.useDefault ?? false, f) ? 'en-US' : f.string.sample() },
            ...!resolveCondition(options?.includeOptional ?? true, f) ? {} : { watch_region: f.string.sample() }
        }
    };
};

export const fakeGet3WatchProvidersTvResponse = (options?: Options): Get3WatchProvidersTvResponse => {
    const f = options?.faker ?? faker;
    return {
        results: f.helpers.multiple(() => ({
            display_priorities: {
                CA: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                AE: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                AR: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                AT: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                AU: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                BE: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                BO: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                BR: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                BG: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                CH: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                CL: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                CO: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                CR: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                CZ: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                DE: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                DK: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                EC: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                EE: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                EG: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                ES: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                FI: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                FR: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                GB: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                GR: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                GT: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                HK: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                HN: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                HU: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                ID: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                IE: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                IN: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                IT: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                JP: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                LT: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                LV: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                MX: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                MY: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                NL: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                NO: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                NZ: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                PE: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                PH: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                PL: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                PT: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                PY: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                RU: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                SA: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                SE: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                SG: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                SK: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                TH: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                TR: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                TW: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                US: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                VE: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                ZA: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                SI: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                CV: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                GH: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                MU: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                MZ: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                UG: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
                IL: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
            },
            display_priority: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int(),
            logo_path: f.string.sample(),
            provider_name: f.string.sample(),
            provider_id: resolveCondition(options?.useDefault ?? false, f) ? 0 : f.number.int()
        }))
    };
};
